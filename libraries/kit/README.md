# @nostrwatch/kit

> **DEPRECATED** — This package was an empty adapter scaffold that was never completed.

## Overview

`@nostrwatch/kit` was a planned adapter toolkit for nostr-watch. The directory contains only an empty adapter scaffold (`adapters/kit-adapter-idb/`) with no source implementation. The package was never developed beyond the initial directory structure.

## Installation

This package is deprecated and contains no implementation. Do not add `@nostrwatch/kit` as a dependency.

## Quick Start

This package is deprecated. No usable code exists. See `@nostrwatch/route66` for the adapter pattern used in nostr-watch.

## Known Limitations

This package is deprecated and contains no implementation.

## License

[MIT](../../LICENSE)
