---
phase: 02-internal-package-readmes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/nwcache/README.md
  - internal/redis/README.md
  - internal/seed/README.md
autonomous: true
requirements:
  - INT-05
  - INT-06
  - INT-09

must_haves:
  truths:
    - "A developer can read internal/nwcache/README.md and immediately see it is deprecated with no replacement"
    - "A developer can read internal/redis/README.md and understand it is a minimal Redis integration wrapper"
    - "A developer can read internal/seed/README.md and understand relay seeding sources and configuration"
    - "All three READMEs pass pnpm lint:docs without errors"
  artifacts:
    - path: "internal/nwcache/README.md"
      provides: "Deprecation stub README for nwcache"
      contains: "DEPRECATED"
    - path: "internal/redis/README.md"
      provides: "Minimal styleguide-conforming README for redis"
      contains: "## Overview"
    - path: "internal/seed/README.md"
      provides: "Full styleguide-conforming README replacing existing non-conforming one"
      contains: "## Overview"
  key_links:
    - from: "internal/nwcache/README.md"
      to: "docs/styleguide/README.md"
      via: "deprecation stub template compliance"
      pattern: "DEPRECATED"
---

<objective>
Write styleguide-conforming README.md files for the three infrastructure internal packages: nwcache (deprecation stub), redis (minimal), and seed (full rewrite of existing README).

Purpose: nwcache is explicitly deprecated (functionality inlined), redis is a minimal wrapper with no description in package.json, and seed has an existing README that does not conform to the styleguide. This plan handles the special cases: deprecation stub, minimal package, and README rewrite.
Output: Three README.md files that pass markdownlint-cli2 and render in VitePress.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/styleguide/README.md
@.markdownlint-cli2.jsonc
@.planning/codebase/CONCERNS.md
@.planning/phases/02-internal-package-readmes/02-CONTEXT.md

<interfaces>
<!-- Deprecation stub template (from styleguide): -->
<!-- H1, blockquote DEPRECATED banner, ## Why deprecated, ## Migrating (or note no replacement), ## License -->
<!-- BUT: deprecation stubs still need to pass MD043 which requires Overview, Installation, Quick Start, Known Limitations, License -->
<!-- IMPORTANT: The deprecation stub must include MD043-required sections to pass linting -->
<!-- Adapt the styleguide deprecation template to include required headings -->

<!-- nwcache special case per CONTEXT.md: "gets a deprecation stub — explicitly noted as deprecated, no replacement — functionality was inlined" -->

<!-- redis has package.json with no description field, contains: CHANGELOG.md, docker-compose.yaml, index.js, package.json -->
<!-- Per CONTEXT.md: "if source directory is empty or has no meaningful exports, write a minimal README" -->

<!-- seed has existing README.md that does NOT conform to styleguide (missing Overview, Quick Start, Known Limitations sections) -->

<!-- Code style: no semicolons, single quotes, ESM imports, TypeScript preferred -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write deprecation stub for internal/nwcache</name>
  <files>internal/nwcache/README.md</files>
  <action>
Read the nwcache package to confirm its state:
- `internal/nwcache/package.json` for metadata
- `internal/nwcache/index.js` and other files to understand what it contained

Write `internal/nwcache/README.md` as a deprecation stub.

CRITICAL: The deprecation stub template from the styleguide (H1 + blockquote banner + Why deprecated + Migrating + License) does NOT include all MD043-required sections (Overview, Installation, Quick Start, Known Limitations). The stub must be adapted to pass linting.

Write the stub with this structure:
1. **Header**: `# @nostrwatch/nwcache` — NO badges per styleguide deprecation template ("no badges")
2. **Deprecation banner**: Blockquote immediately after H1: `> **DEPRECATED** — This package is no longer maintained. Its caching functionality was inlined into consuming packages. No replacement package exists.`
3. **Overview**: Brief statement that nwcache was an internal caching layer. State it is deprecated — functionality was inlined into consuming packages.
4. **Installation**: State this package is deprecated and should not be installed.
5. **Quick Start**: State this package is deprecated. Do not install or use.
6. **Why deprecated**: One paragraph explaining the caching functionality was inlined. Per CONTEXT.md: "nwcache gets a deprecation stub — functionality was inlined."
7. **Known Limitations**: "This package is deprecated and no longer maintained."
8. **License**: `[MIT](../../LICENSE)`

The key challenge: merge the styleguide deprecation stub format with MD043 compliance. Every required heading must appear.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 internal/nwcache/README.md</automated>
  </verify>
  <done>internal/nwcache/README.md exists as deprecation stub, passes markdownlint-cli2, prominently displays DEPRECATED banner, states functionality was inlined with no replacement</done>
</task>

<task type="auto">
  <name>Task 2: Write minimal README for internal/redis</name>
  <files>internal/redis/README.md</files>
  <action>
Read the redis package:
- `internal/redis/package.json` for metadata (has name @nostrwatch/redis, version 0.1.0, no description)
- `internal/redis/index.js` for exports
- `internal/redis/docker-compose.yaml` for Redis service config
- Per CONTEXT.md: redis may have no meaningful exports — "write a minimal README (overview + installation + license)"

Write `internal/redis/README.md`:

1. **Header**: `# @nostrwatch/redis` with scope badge (internal), license badge, status badge (alpha)
2. **Overview**: Infer purpose from source — likely provides Redis client configuration/connection for the monorepo. Describe what it wraps (ioredis, redis, or similar). If index.js just re-exports a Redis client, say so. Mention the docker-compose.yaml for local Redis.
3. **Installation**: Internal workspace pattern.
4. **Quick Start**: If there are meaningful exports, show basic usage. If minimal (just a pre-configured Redis client), show the import and connection pattern. Keep brief.
5. **Configuration**: If docker-compose.yaml contains Redis config, document the local development setup.
6. **Known Limitations**: Check source. If minimal package, "No known limitations at this time."
7. **License**: `[MIT](../../LICENSE)`

MUST include all MD043-required sections even if content is brief.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 internal/redis/README.md</automated>
  </verify>
  <done>internal/redis/README.md exists, passes markdownlint-cli2, accurately describes Redis integration purpose, all required sections present</done>
</task>

<task type="auto">
  <name>Task 3: Rewrite README for internal/seed</name>
  <files>internal/seed/README.md</files>
  <action>
Read the seed package source for accurate documentation:
- `internal/seed/src/index.ts` for exports
- `internal/seed/src/config.ts` for configuration
- `internal/seed/src/types.d.ts` for type definitions
- `internal/seed/src/utils/` for utility functions
- `internal/seed/package.json` for metadata ("Nostr relay seeder and discovery module")
- The EXISTING `internal/seed/README.md` — read it to understand current content, but rewrite completely to conform to styleguide

Rewrite `internal/seed/README.md` from scratch following the styleguide:

1. **Header**: `# @nostrwatch/seed` with scope badge (internal), license badge, status badge (alpha)
2. **Overview**: Relay seeding and discovery module — collects relay URLs from multiple sources (config, static files, databases, APIs, Nostr events, cache). Explain the seeding concept: how monitors need an initial list of relays to check, and seed provides that bootstrap.
3. **Prerequisites**: List any external requirements (database, Redis, API endpoints).
4. **Installation**: Internal workspace pattern.
5. **Quick Start**: Show realistic usage with TypeScript. The existing README has a good usage example but uses JavaScript and semicolons — convert to TypeScript, no semicolons, single quotes per styleguide. Show the RelaySeeder creation, start, and getRelays pattern.
6. **API**: Document RelaySeeder class, configuration options, source types (config, static, db, api, events, cache), allowed networks (clearnet, tor, i2p).
7. **Configuration**: Document the full configuration schema — sources array, options object, interval, allowedNetworks, isRelayBlocked callback.
8. **Known Limitations**: Scan source for TODOs. If none notable, "No known limitations at this time."
9. **Agent Skills**: "No agent skills defined yet for this package."
10. **Related Packages**: Link to apps/relaymon (primary consumer), internal/redis (may use for caching).
11. **License**: `[MIT](../../LICENSE)`

The existing README has good content but wrong structure (missing Overview, Quick Start, Known Limitations headings; uses JavaScript not TypeScript; has semicolons). Extract the valuable information and restructure.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 internal/seed/README.md</automated>
  </verify>
  <done>internal/seed/README.md rewritten from scratch, passes markdownlint-cli2, documents seeding sources and configuration thoroughly, TypeScript examples follow code conventions</done>
</task>

</tasks>

<verification>
Run markdownlint-cli2 across all three files:

```sh
cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 internal/nwcache/README.md internal/redis/README.md internal/seed/README.md
```

All three must pass with zero errors.
</verification>

<success_criteria>
- Three README.md files created/rewritten in internal/nwcache/, internal/redis/, internal/seed/
- All pass markdownlint-cli2
- nwcache README is a deprecation stub with DEPRECATED banner and all required sections for MD043
- redis README is minimal but conforming, accurately describes the Redis integration
- seed README is a complete rewrite conforming to styleguide with TypeScript examples
- Code examples follow styleguide conventions (no semicolons, single quotes, ESM imports, TypeScript)
</success_criteria>

<output>
After completion, create `.planning/phases/02-internal-package-readmes/02-03-SUMMARY.md`
</output>
