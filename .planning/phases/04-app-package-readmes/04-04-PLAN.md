---
phase: 04-app-package-readmes
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/nocapd/README.md
  - apps/umon/README.md
autonomous: true
requirements:
  - APP-06
  - APP-08

must_haves:
  truths:
    - "A developer opening apps/nocapd/README.md immediately sees a prominent deprecation notice with a link to relaymon as the replacement"
    - "A developer reading apps/umon/README.md understands it is a Manifest V3 browser extension for monitoring Nostr activity, how to build it with pnpm build, and how to load it as an unpacked extension"
    - "Both READMEs pass markdownlint-cli2 (MD043 section order) without modification"
    - "nocapd deprecation stub contains all MD043-required sections (Overview, Installation, Quick Start, Known Limitations, License) even though content is N/A for most"
    - "umon README notes alpha status, Svelte 4 usage, and no web store listing in Known Limitations"
  artifacts:
    - path: "apps/nocapd/README.md"
      provides: "Deprecation stub directing users to relaymon"
      contains: "DEPRECATED"
    - path: "apps/umon/README.md"
      provides: "Browser extension README with build instructions and extension loading guide"
      contains: "## Overview"
  key_links:
    - from: "apps/nocapd/README.md"
      to: "apps/relaymon/README.md"
      via: "Deprecation redirect link"
      pattern: "relaymon"
    - from: "apps/nocapd/README.md"
      to: "docs/styleguide/README.md"
      via: "MD043 heading order compliance"
      pattern: "## Overview.*## Installation.*## Quick Start.*## Known Limitations.*## License"
    - from: "apps/umon/README.md"
      to: "docs/styleguide/README.md"
      via: "MD043 heading order compliance"
      pattern: "## Overview.*## Installation.*## Quick Start.*## Known Limitations.*## License"
---

<objective>
Write a deprecation stub for apps/nocapd and a full README for apps/umon (browser extension).

Purpose: nocapd is a deprecated relay monitoring daemon replaced by relaymon -- it needs a proper deprecation stub that passes MD043 linting. umon is an early-stage Manifest V3 browser extension with actual source code that needs its first README written from scratch.
Output: Two styleguide-conforming README.md files that pass MD043 linting.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-app-package-readmes/04-RESEARCH.md
@.planning/codebase/CONCERNS.md
@docs/styleguide/README.md
@.markdownlint-cli2.jsonc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write apps/nocapd deprecation stub README.md</name>
  <files>apps/nocapd/README.md</files>
  <action>
Write a deprecation stub README for apps/nocapd. The existing README has a partial deprecation notice but does not follow the styleguide template and fails MD043. Replace it entirely with a proper deprecation stub.

**Package details (confirmed from existing README and styleguide):**
- npm name: `@nostrwatch/nocapd`
- Status: DEPRECATED -- replaced by relaymon
- The styleguide (docs/styleguide/README.md) has a concrete nocapd example in the Deprecation Stub Template section. Use that content as the base.

**CRITICAL: MD043 compliance for deprecation stubs.** From Phase 2-03 decision: deprecation stubs MUST include all MD043-required sections even though the styleguide template omits some. The stub needs ## Overview, ## Installation, ## Quick Start, ## Known Limitations, ## License with "N/A -- this package is deprecated" content where appropriate. Insert "## Why deprecated" and "## Migrating" as optional sections between required ones.

**Required MD043 section order:** H1, ## Overview, ## Installation, ## Quick Start, ## Known Limitations, ## License

**Section content:**

H1: `# @nostrwatch/nocapd`

Immediately after H1, the deprecation blockquote banner:
```
> **DEPRECATED** -- This package has been replaced by [@nostrwatch/relaymon](../relaymon/README.md). No further updates will be made here.
```

## Overview: Brief statement that nocapd was an early relay monitoring daemon. State it is deprecated and replaced by relaymon.

## Installation: "N/A -- this package is deprecated. See [@nostrwatch/relaymon](../relaymon/README.md)."

## Quick Start: "N/A -- this package is deprecated. See [@nostrwatch/relaymon](../relaymon/README.md)."

## Why deprecated: Content from styleguide concrete example -- nocapd was an early relay monitoring daemon that checked relay health on a polling schedule. It has been replaced by relaymon, which provides more robust and configurable monitoring, better database integration, and improved NIP-66 event publishing.

## Migrating: Use relaymon instead. Configuration format has changed -- see the relaymon README for the new environment variable schema. Link: `[@nostrwatch/relaymon](../relaymon/README.md)`.

## Known Limitations: "N/A -- this package is deprecated."

## License: `[MIT](../../LICENSE)`

**Anti-patterns to avoid:**
- Do NOT keep the existing README content (config YAML, env var docs) -- the stub replaces everything
- Do NOT omit any MD043-required section (## Overview, ## Installation, ## Quick Start, ## Known Limitations, ## License)
- Do NOT use badges -- deprecation stubs do not need badges per styleguide convention (Phase 2-03 pattern: stubs used no badges)
- Ensure blank lines around all headings (MD022)
- Use relative link `../relaymon/README.md` (both are in apps/ -- sibling directories)
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && pnpm lint:docs 2>&1 | grep -E "nocapd|error|warning" || echo "lint passed"</automated>
  </verify>
  <done>apps/nocapd/README.md exists as a proper deprecation stub, passes markdownlint-cli2 MD043, contains prominent DEPRECATED banner, links to relaymon as replacement, includes all MD043-required sections</done>
</task>

<task type="auto">
  <name>Task 2: Write apps/umon README.md</name>
  <files>apps/umon/README.md</files>
  <action>
Write a complete styleguide-conforming README for apps/umon. There is NO existing README -- write from scratch based on research findings.

**Package details (confirmed from manifest.json, package.json, and source):**
- npm name: `@nicfab/umon` (from package.json -- verify; if no package.json name, use `@nostrwatch/umon`)
- Version: 0.1.0
- Tech: Manifest V3 browser extension, Svelte 4, Rollup, TypeScript
- Runtime badge: browser
- Status badge: alpha
- Source structure:
  - `src/background/index.ts` -- service worker background script
  - `src/popup/` -- popup UI (Svelte 4, Popup.svelte)
  - `src/utils/secureStorage.ts`, `src/utils/storage.ts` -- storage utilities
- Build: rollup (rollup.config.js)
- NOT an npm-published package; it is a browser extension
- NOT deprecated; has actual source code at v0.1.0

**Required MD043 section order:** H1, ## Overview, ## Installation, ## Quick Start, ## Known Limitations, ## License

**Section content:**

H1: `# @nostrwatch/umon`
- Tagline: "Browser extension for monitoring Nostr relay activity."
- Badges: Scope app (not npm version since it is a browser extension), License MIT, Status alpha, Runtime browser

## Overview (2-3 sentences): A Manifest V3 browser extension for monitoring Nostr activity. Provides a popup interface built with Svelte 4 and uses a background service worker for persistent monitoring. Currently in early development (v0.1.0).

## Prerequisites:

**For development:**
- Node.js >=18
- pnpm

**For end users:**
- Chrome/Chromium or Firefox with Manifest V3 support

## Installation:

**Development setup:**
```sh
cd apps/umon
pnpm install
```

**Browser installation:**
Load the built extension as an unpacked extension in your browser's developer mode (no web store listing available).

## Quick Start:
```sh
# Build the extension
cd apps/umon
pnpm build

# Load in Chrome:
# 1. Navigate to chrome://extensions/
# 2. Enable "Developer mode"
# 3. Click "Load unpacked"
# 4. Select the apps/umon/dist/ directory
```

## Configuration: No documented configuration. The extension stores settings via the browser extension storage API (`src/utils/secureStorage.ts`, `src/utils/storage.ts`).

## Known Limitations:
- **Alpha/early stage** -- v0.1.0 with minimal feature set; expect breaking changes
- **No web store listing** -- Must be loaded as an unpacked extension in developer mode
- **Uses Svelte 4** -- Other packages in the monorepo use Svelte 5; this extension has not been migrated
- **Minimal test coverage** -- Jest scaffolding present but limited test suite

## Agent Skills: "No skills defined yet" placeholder.

## Related Packages: None confirmed from workspace dependencies.

## License: `[MIT](../../LICENSE)`

**Anti-patterns to avoid:**
- Do NOT use npm version badge (this is a browser extension, not published to npm)
- Do NOT fabricate features beyond what is observable from manifest.json and source
- Do NOT describe installation via a web store (none exists)
- Ensure blank lines around all headings (MD022)
- Every fenced code block needs a language tag (MD040)
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && pnpm lint:docs 2>&1 | grep -E "umon|error|warning" || echo "lint passed"</automated>
  </verify>
  <done>apps/umon/README.md exists, passes markdownlint-cli2 MD043, documents Manifest V3 browser extension nature, includes build and load-unpacked instructions, notes alpha status and Svelte 4 in Known Limitations</done>
</task>

</tasks>

<verification>
Run `pnpm lint:docs` from monorepo root. Both apps/nocapd/README.md and apps/umon/README.md must pass with zero errors. Verify nocapd README has prominent DEPRECATED banner and links to relaymon. Verify umon README documents browser extension build/load workflow.
</verification>

<success_criteria>
- apps/nocapd/README.md exists as a deprecation stub and passes `pnpm lint:docs`
- nocapd stub has DEPRECATED banner with link to relaymon
- nocapd stub contains all MD043-required sections (Overview, Installation, Quick Start, Known Limitations, License)
- apps/umon/README.md exists and passes `pnpm lint:docs`
- umon README documents Manifest V3 extension, pnpm build, and unpacked extension loading
- umon Known Limitations notes alpha status, no web store, Svelte 4, minimal tests
</success_criteria>

<output>
After completion, create `.planning/phases/04-app-package-readmes/04-04-SUMMARY.md`
</output>
