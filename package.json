{
  "name": "nostrwatch-monorepo",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "bootstrap": "lerna bootstrap",
    "clean": "lerna clean",
    "test": "lerna run test"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
