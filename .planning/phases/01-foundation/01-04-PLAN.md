---
phase: 01-foundation
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - docs/.vitepress/packages.data.ts
autonomous: true
gap_closure: true
requirements:
  - FOUND-05
  - FOUND-08

must_haves:
  truths:
    - "Running `pnpm lint:docs` from the repo root invokes markdownlint-cli2 successfully (exits 0 on conforming READMEs or exits non-zero with rule violation details on non-conforming READMEs)"
    - "The VitePress data loader discovers all package directories including those without package.json, producing 30+ entries in the discovery index"
  artifacts:
    - path: "package.json"
      provides: "markdownlint-cli2 in devDependencies"
      contains: "markdownlint-cli2"
    - path: "docs/.vitepress/packages.data.ts"
      provides: "Data loader that discovers packages by directory presence, not just package.json presence"
  key_links:
    - from: "package.json"
      to: "node_modules/.bin/markdownlint-cli2"
      via: "pnpm install resolves devDependency"
      pattern: "markdownlint-cli2"
    - from: "docs/.vitepress/packages.data.ts"
      to: "{apps,libraries,internal}/*/package.json AND directories without package.json"
      via: "glob for directories + optional package.json read"
      pattern: "glob.*apps.*libraries.*internal"
---

<objective>
Close two verification gaps from Phase 1:

1. **FOUND-05 gap:** `pnpm lint:docs` fails with "command not found" because markdownlint-cli2 is not in devDependencies. Fix by adding the dependency and running pnpm install.

2. **FOUND-08 gap:** The package discovery index data loader only finds 28 packages because 10 package directories lack package.json. Fix by updating the data loader to discover directories first, then optionally read package.json for metadata, falling back to directory name when no manifest exists.

Purpose: Restore local/CI parity for linting and ensure the discovery index meets the "30+ packages" requirement.
Output: Working `pnpm lint:docs` command; data loader that discovers all 38 package directories.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add markdownlint-cli2 to devDependencies and install</name>
  <files>package.json</files>
  <action>
    Add `"markdownlint-cli2": "^0.17.0"` to the `devDependencies` object in the root `package.json`.

    Use the latest 0.x version available. The research noted v0.21.0 was present via npx but it is not in devDependencies. Add it so `pnpm lint:docs` resolves the binary from node_modules.

    After editing package.json, run `pnpm install` to install the dependency and update the lockfile.

    Verify the binary is available: `npx markdownlint-cli2 --help` should print usage info.
  </action>
  <verify>
    <automated>pnpm lint:docs 2>&1; echo "EXIT: $?"</automated>
  </verify>
  <done>
    `pnpm lint:docs` executes markdownlint-cli2 (not "command not found"). It either exits 0 (all READMEs conform) or exits non-zero with specific rule violation details. The command must not fail with a missing binary error.
  </done>
</task>

<task type="auto">
  <name>Task 2: Update data loader to discover all package directories</name>
  <files>docs/.vitepress/packages.data.ts</files>
  <action>
    The current data loader globs for `{apps,libraries,internal}/*/package.json` — this misses 10 directories that have no package.json (docker-stacks, purist, relaymon, trawler, idb, kit, nip66, sanitize, transform, kinds).

    Rewrite the data loader to:
    1. Glob for all directories: `{apps,libraries,internal}/*/` (directories, not package.json files)
    2. For each directory, check if `package.json` exists
    3. If it exists, read it and extract name, description, version, status, deprecated as before
    4. If it does NOT exist, fall back to sensible defaults:
       - `name`: directory name (the slug, e.g., "docker-stacks")
       - `description`: empty string
       - `version`: empty string
       - `status`: "alpha" (per existing default)
       - `deprecated`: false

    Use `fs.existsSync` to check for package.json before reading.

    Keep the existing sort by name and all existing fields (name, description, version, status, type, slug, deprecated, link).

    The 10 missing directories are:
    - apps/docker-stacks, apps/purist, apps/relaymon, apps/trawler
    - libraries/idb, libraries/kit, libraries/nip66, libraries/sanitize, libraries/transform
    - internal/kinds
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && node -e "
      import('./docs/.vitepress/packages.data.ts').catch(() => {
        // ESM/TS won't load directly, so verify via glob count instead
        const { globSync } = require('glob');
        const { existsSync } = require('fs');
        const dirs = globSync('{apps,libraries,internal}/*/', { cwd: process.cwd() })
          .map(d => d.replace(/\/$/, ''));
        console.log('Total directories:', dirs.length);
        const withPkg = dirs.filter(d => existsSync(d + '/package.json'));
        console.log('With package.json:', withPkg.length);
        const withoutPkg = dirs.filter(d => !existsSync(d + '/package.json'));
        console.log('Without package.json:', withoutPkg.length);
        console.log('Missing:', withoutPkg.join(', '));
        if (dirs.length >= 30) { console.log('PASS: 30+ directories found'); process.exit(0); }
        else { console.log('FAIL: fewer than 30 directories'); process.exit(1); }
      });
    " 2>/dev/null || node -e "
      const { globSync } = require('glob');
      const { existsSync } = require('fs');
      const dirs = globSync('{apps,libraries,internal}/*/', { cwd: process.cwd() })
        .map(d => d.replace(/\/$/, ''));
      console.log('Total directories:', dirs.length);
      if (dirs.length >= 30) { console.log('PASS: 30+ directories found'); process.exit(0); }
      else { console.log('FAIL: fewer than 30 directories'); process.exit(1); }
    "</automated>
  </verify>
  <done>
    The data loader discovers all 38 package directories (or however many exist). Directories without package.json appear in the output with fallback metadata. The discovery index will show 30+ packages when rendered by VitePress.
  </done>
</task>

</tasks>

<verification>
1. `pnpm lint:docs` runs without "command not found" — markdownlint-cli2 binary resolves from node_modules
2. The data loader file (`docs/.vitepress/packages.data.ts`) uses directory globbing, not package.json globbing, ensuring all directories are discovered
3. Both FOUND-05 and FOUND-08 gaps from 01-VERIFICATION.md are resolved
</verification>

<success_criteria>
- `pnpm lint:docs` invokes markdownlint-cli2 successfully (FOUND-05 gap closed)
- Data loader discovers 30+ package directories including those without package.json (FOUND-08 gap closed)
- No regressions: existing packages with package.json still show correct metadata (name, description, status from their manifest)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
