---
phase: 03-library-package-readmes
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - libraries/nocap/README.md
autonomous: true
requirements:
  - LIB-01

must_haves:
  truths:
    - "A developer can read the nocap README and understand what an adapter is, what interface it must implement, and how to register it — without reading source code"
    - "The README documents all five adapter types (websocket, dns, geo, info, ssl) with their required methods"
    - "The README shows how to create a custom adapter by extending AbstractAdapter"
    - "The README shows the useAdapter/useAdapters registration API"
    - "The README passes pnpm lint:docs without errors"
  artifacts:
    - path: "libraries/nocap/README.md"
      provides: "Gold-standard adapter documentation for nocap relay capability checking"
      contains: "## Overview"
      min_lines: 100
  key_links:
    - from: "libraries/nocap/README.md"
      to: "libraries/nocap/adapters/"
      via: "Adapter Pattern section references existing adapter implementations"
      pattern: "adapter"
    - from: "libraries/nocap/README.md"
      to: "libraries/nocap/src/classes/AbstractAdapter.ts"
      via: "API section documents AbstractAdapter base class"
      pattern: "AbstractAdapter"
---

<objective>
Write the gold-standard adapter documentation README for libraries/nocap — the adapter-based relay capability discovery system.

Purpose: nocap is the primary extension mechanism in the monorepo. Its README is a phase success criterion: a developer must understand adapters without reading source code. This plan requires deep source reading of the adapter interfaces, base class, registration API, and all five default adapter implementations.
Output: One comprehensive README.md that passes markdownlint-cli2 and serves as the reference for adapter pattern documentation.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/styleguide/README.md
@.markdownlint-cli2.jsonc
@.planning/phases/03-library-package-readmes/03-CONTEXT.md
@.planning/phases/03-library-package-readmes/03-RESEARCH.md
@.planning/phases/02-internal-package-readmes/02-02-SUMMARY.md

<interfaces>
<!-- nocap adapter system (from 03-RESEARCH.md, verified from source): -->

<!-- Base class: AbstractAdapter in libraries/nocap/src/classes/AbstractAdapter.ts -->
<!-- Interfaces: IAdapter, IAdapterConstructor, AdapterType in libraries/nocap/src/interfaces/ -->
<!-- Per-type interfaces: IWebsocketAdapter, IDnsAdapter, IInfoAdapter, ISslAdapter, IGeoAdapter -->

<!-- Registration API (from Base.ts): -->
<!-- nocap.useAdapter(AdapterClass) — register single adapter -->
<!-- nocap.useAdapters([AdapterClass1, AdapterClass2]) — register multiple adapters -->
<!-- nocap.check('dns') or nocap.check(['open', 'read', 'dns']) — run checks -->

<!-- Five adapter types and their required methods: -->
<!-- websocket: check_open(), check_read(), check_write() -->
<!-- dns: check_dns() -->
<!-- info: check_info() -->
<!-- ssl: check_ssl() -->
<!-- geo: check_geo() -->

<!-- Existing default adapters (in libraries/nocap/adapters/default/): -->
<!-- DnsAdapterDefault — DNS lookup via Cloudflare DNS-over-HTTPS -->
<!-- GeoAdapterDefault — Geographic IP lookup -->
<!-- InfoAdapterDefault — NIP-11 info document fetch -->
<!-- SslAdapterDefault — TLS certificate validation -->
<!-- WebsocketAdapterDefault — WebSocket open/read/write checks -->

<!-- Custom adapter pattern: -->
<!-- export class MyAdapter extends AbstractAdapter implements IAdapter { -->
<!--   static type: AdapterType = 'dns' -->
<!--   readonly slug: string = 'MyAdapter' -->
<!--   initialize(): void {} -->
<!--   async check_dns(): Promise<void> { this.base.finish('dns', result) } -->
<!-- } -->

<!-- Adapter documentation pattern (from Phase 2 publisher README): -->
<!-- 1. What the adapter does -->
<!-- 2. Base class to extend -->
<!-- 3. Interface to implement -->
<!-- 4. Registration mechanism -->
<!-- 5. Existing adapter list -->

<!-- nocap package: @nostrwatch/nocap v0.9.1, no existing README -->
<!-- Code style: no semicolons, single quotes, ESM imports, TypeScript -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write comprehensive README for libraries/nocap</name>
  <files>libraries/nocap/README.md</files>
  <action>
Read nocap source thoroughly — this README must be the gold standard:
- `libraries/nocap/package.json` for metadata (@nostrwatch/nocap v0.9.1)
- `libraries/nocap/src/index.ts` for public exports
- `libraries/nocap/src/classes/Base.ts` for Nocap class constructor, useAdapter/useAdapters, check() API
- `libraries/nocap/src/classes/AbstractAdapter.ts` for AbstractAdapter base class
- `libraries/nocap/src/interfaces/IAdapter.ts` for IAdapter interface
- `libraries/nocap/src/interfaces/IAdapterConstructor.ts` for adapter constructor contract
- `libraries/nocap/src/interfaces/IWebsocketAdapter.ts` for websocket adapter interface
- `libraries/nocap/src/interfaces/IDnsAdapter.ts`, `IInfoAdapter.ts`, `ISslAdapter.ts`, `IGeoAdapter.ts`
- `libraries/nocap/adapters/default/DnsAdapterDefault/src/index.ts` as concrete adapter example
- `libraries/nocap/adapters/default/WebsocketAdapterDefault/src/index.ts` for websocket adapter example

Write `libraries/nocap/README.md` following the styleguide with adapter documentation depth:

1. **Header**: `# @nostrwatch/nocap` with npm badge (nocap), license badge, status badge (alpha), runtime badge (node)
2. **Overview**: Adapter-based relay capability discovery. Checks relay health across multiple dimensions (WebSocket connectivity, DNS resolution, SSL certificates, NIP-11 info, geolocation) using a pluggable adapter architecture. Define "relay" and "adapter" inline. Explain that nocap provides the checking framework — adapters provide the actual check implementations. This allows swapping check implementations without changing application code.
3. **Installation**: `pnpm add @nostrwatch/nocap` + npm alternative
4. **Quick Start**: Show the full usage pattern from 03-RESEARCH.md code examples:
   - Import Nocap and default adapters
   - Create Nocap instance with relay URL
   - Register adapters with useAdapters
   - Run checks
   - Log results
   TypeScript, no semicolons, single quotes.
5. **API**: Document:
   - `Nocap` class: constructor(url: string), useAdapter(adapter), useAdapters([adapters]), check(keys)
   - Return type from check() — the result object structure
   - `AbstractAdapter` base class: what it provides to subclasses
   - Adapter type routing: how `static type` dispatches to the correct adapter
6. **Adapter Pattern**: Dedicated subsection (per CONTEXT.md decision). Follow the Phase 2 publisher adapter documentation pattern:
   a. What adapters do — one paragraph explaining the adapter concept
   b. Base class to extend — `AbstractAdapter` with TypeScript signature
   c. Interface to implement — `IAdapter` with required properties/methods
   d. Per-type interfaces — table of all 5 types with their required methods:
      | Type | Interface | Required Methods |
      | websocket | IWebsocketAdapter | check_open(), check_read(), check_write() |
      | dns | IDnsAdapter | check_dns() |
      | info | IInfoAdapter | check_info() |
      | ssl | ISslAdapter | check_ssl() |
      | geo | IGeoAdapter | check_geo() |
   e. Registration mechanism — `useAdapter()` and `useAdapters()`
   f. Creating a custom adapter — full working example extending AbstractAdapter (use the custom DNS adapter example from 03-RESEARCH.md)
   g. Existing adapters — bulleted list with one-line descriptions:
      - DnsAdapterDefault — DNS lookup via Cloudflare DNS-over-HTTPS (1.1.1.1)
      - GeoAdapterDefault — Geographic IP lookup
      - InfoAdapterDefault — NIP-11 info document fetch
      - SslAdapterDefault — TLS certificate validation
      - WebsocketAdapterDefault — WebSocket open/read/write checks
   h. Link to `libraries/nocap/adapters/default/` as reference implementations
7. **Known Limitations**: "No known limitations at this time." (no CONCERNS.md entries for nocap)
8. **Agent Skills**: "No agent skills defined yet for this package."
9. **Related Packages**: Link to libraries/nocap-route66 (transforms nocap output to NIP-66 events), libraries/auditor (uses nocap for relay testing), internal/publisher (publishes check results), libraries/route66 (orchestrates nocap checks)
10. **License**: `[MIT](../../LICENSE)`

This README MUST be thorough enough that a developer can create a new adapter without reading source code. Test this by verifying: the base class is documented, the interface methods are listed, the registration API is shown, and a complete custom adapter example is included.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/nocap/README.md</automated>
  </verify>
  <done>libraries/nocap/README.md exists, passes markdownlint-cli2, documents all five adapter types with required methods, shows custom adapter creation example, shows useAdapter/useAdapters registration API, a developer can create a new adapter without reading source code</done>
</task>

</tasks>

<verification>
Run markdownlint-cli2:

```sh
cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/nocap/README.md
```

Must pass with zero errors.

Content verification: Confirm the README contains:
- AbstractAdapter base class documentation
- All five adapter type interfaces with their required methods
- A complete custom adapter code example
- useAdapter/useAdapters registration API
- List of existing default adapters
</verification>

<success_criteria>
- libraries/nocap/README.md exists and passes markdownlint-cli2
- README is comprehensive enough for adapter creation without source reading (phase success criterion)
- All five adapter types documented with required methods
- Custom adapter creation example is complete and runnable
- Registration API (useAdapter/useAdapters) clearly documented
- Existing adapters listed with descriptions
- Code examples follow styleguide conventions
</success_criteria>

<output>
After completion, create `.planning/phases/03-library-package-readmes/03-03-SUMMARY.md`
</output>
