---
phase: 03-library-package-readmes
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - libraries/worker-relay/README.md
  - libraries/negentropy/README.md
  - libraries/idb/README.md
  - libraries/kit/README.md
  - libraries/sanitize/README.md
  - libraries/transform/README.md
autonomous: true
requirements:
  - LIB-16
  - LIB-15
  - LIB-10

must_haves:
  truths:
    - "A developer can read worker-relay README and understand WorkerRelayInterface, sqlite-wasm, and OPFS persistence"
    - "A developer can read negentropy README and understand it implements NIP-77 (not NIP-49) set reconciliation"
    - "idb, kit, sanitize, and transform directories each have a deprecation stub README that passes markdownlint-cli2"
    - "All six READMEs pass pnpm lint:docs without errors"
  artifacts:
    - path: "libraries/worker-relay/README.md"
      provides: "Styleguide-conforming README for web worker relay"
      contains: "## Overview"
    - path: "libraries/negentropy/README.md"
      provides: "Styleguide-conforming README for NIP-77 negentropy sync"
      contains: "## Overview"
    - path: "libraries/idb/README.md"
      provides: "Deprecation stub for empty IndexedDB directory"
      contains: "## Overview"
    - path: "libraries/kit/README.md"
      provides: "Deprecation stub for empty adapter scaffold"
      contains: "## Overview"
    - path: "libraries/sanitize/README.md"
      provides: "Deprecation stub for empty sanitize directory"
      contains: "## Overview"
    - path: "libraries/transform/README.md"
      provides: "Deprecation stub for empty transform directory"
      contains: "## Overview"
  key_links: []
---

<objective>
Write styleguide-conforming README.md files for worker-relay (web worker relay) and negentropy (NIP-77 set reconciliation), plus deprecation stubs for idb, kit, sanitize, and transform (empty/defunct directories).

Purpose: worker-relay and negentropy are real libraries with existing wrong-format READMEs that need styleguide conformance. The four deprecation stubs cover directories with no active source — they must still pass MD043 enforcement. Bundling the stubs into a single task keeps the plan efficient.
Output: Six README.md files that pass markdownlint-cli2 and render in VitePress.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/styleguide/README.md
@.markdownlint-cli2.jsonc
@.planning/phases/03-library-package-readmes/03-CONTEXT.md
@.planning/phases/03-library-package-readmes/03-RESEARCH.md

<interfaces>
<!-- worker-relay: existing README 45 lines wrong format (starts with ## not #) -->
<!-- worker-relay package: @nostrwatch/worker-relay v1.3.0 -->
<!-- worker-relay: WorkerRelayInterface class, sqlite-wasm, OPFS persistence, NIP-119 -->

<!-- negentropy: existing README 150 lines wrong format (uses yarn) -->
<!-- negentropy package: @nostrwatch/negentropy-utils -->
<!-- negentropy: implements NIP-77 (NOT NIP-49 as requirements say) -->
<!-- negentropy: ClientHandler/ServerHandler sync protocol -->

<!-- idb: NO source, NO package.json — empty directory with only node_modules -->
<!-- kit: NO source — only empty adapter scaffold (libraries/kit/adapters/kit-adapter-idb/) -->
<!-- sanitize: NO source — only node_modules -->
<!-- transform: source files exist but ALL empty (0 bytes) -->

<!-- Deprecation stub pattern (from Phase 2 nwcache): -->
<!-- H1 + blockquote DEPRECATED banner + Overview + Installation (N/A) + Quick Start (N/A) + Known Limitations (deprecated) + License -->
<!-- MUST include all MD043 required sections even in stubs -->

<!-- Code style: no semicolons, single quotes, ESM imports, TypeScript -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README for libraries/worker-relay</name>
  <files>libraries/worker-relay/README.md</files>
  <action>
Read worker-relay source:
- `libraries/worker-relay/package.json` for metadata (@nostrwatch/worker-relay v1.3.0)
- `libraries/worker-relay/src/index.ts` or main source file for WorkerRelayInterface class
- Existing README at `libraries/worker-relay/README.md` (45 lines) for accurate content
- Look for sqlite-wasm integration, OPFS persistence, NIP-119 references

Write `libraries/worker-relay/README.md` following the styleguide:

1. **Header**: `# @nostrwatch/worker-relay` — starts with H1 NOT H2 (fix existing bug). npm badge, license badge, status badge (alpha), runtime badge (browser)
2. **Overview**: In-browser Nostr relay running in a Web Worker with SQLite-wasm storage and OPFS persistence. Offloads relay event processing from the main thread for better UI performance. Mention NIP-119 if relevant. Define "relay" and "Web Worker" inline.
3. **Installation**: `pnpm add @nostrwatch/worker-relay` + npm alternative
4. **Quick Start**: Show creating a WorkerRelayInterface, connecting, and querying events. TypeScript, no semicolons.
5. **API**: Document WorkerRelayInterface class — constructor, lifecycle methods, event query/storage methods. Note OPFS persistence behavior.
6. **Known Limitations**: "No known limitations at this time." (no CONCERNS.md entries)
7. **Agent Skills**: "No agent skills defined yet for this package."
8. **Related Packages**: Link to libraries/memory-relay (non-persistent alternative), apps/gui (uses worker-relay for in-browser storage)
9. **License**: `[MIT](../../LICENSE)`

CRITICAL: Existing README starts with ## (H2) instead of # (H1) — this is an MD001 violation. The new README MUST start with # (H1).
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/worker-relay/README.md</automated>
  </verify>
  <done>libraries/worker-relay/README.md exists, passes markdownlint-cli2, starts with H1 (not H2), documents WorkerRelayInterface with sqlite-wasm and OPFS</done>
</task>

<task type="auto">
  <name>Task 2: Write README for libraries/negentropy</name>
  <files>libraries/negentropy/README.md</files>
  <action>
Read negentropy source:
- `libraries/negentropy/package.json` for metadata (likely @nostrwatch/negentropy-utils)
- `libraries/negentropy/src/index.ts` for public exports
- Look for ClientHandler and ServerHandler classes
- Existing README at `libraries/negentropy/README.md` (150 lines, uses yarn) for accurate content

Write `libraries/negentropy/README.md` following the styleguide:

1. **Header**: `# @nostrwatch/negentropy-utils` (use actual package name from package.json). npm badge, license badge, status badge (alpha), runtime badge (node)
2. **Overview**: Utilities for NIP-77 (Negentropy) set reconciliation. Provides ClientHandler and ServerHandler for efficient bidirectional synchronization of Nostr event sets between clients and relays. Define "NIP-77" and "negentropy" (set reconciliation protocol) inline. IMPORTANT: This implements NIP-77 (Negentropy), NOT NIP-49 (Private Key Encryption) as incorrectly labeled in requirements. Link to [NIP-77 spec](https://github.com/nostr-protocol/nips/blob/master/77.md).
3. **Installation**: `pnpm add @nostrwatch/negentropy-utils` + npm alternative. NOT yarn.
4. **Quick Start**: Show ClientHandler or ServerHandler usage for syncing event sets. TypeScript, no semicolons. Preserve accurate content from existing README but rewrite in TypeScript with pnpm.
5. **API**: Document ClientHandler and ServerHandler with their key methods. Preserve accurate API content from existing README.
6. **Known Limitations**: "No known limitations at this time." (no CONCERNS.md entries)
7. **Agent Skills**: "No agent skills defined yet for this package."
8. **Related Packages**: Link to libraries/route66 (uses negentropy for sync), libraries/websocket (WebSocket transport)
9. **License**: `[MIT](../../LICENSE)`

CRITICAL: Replace all `yarn add` with `pnpm add` in the new README.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/negentropy/README.md</automated>
  </verify>
  <done>libraries/negentropy/README.md exists, passes markdownlint-cli2, correctly identifies as NIP-77 (not NIP-49), uses pnpm not yarn, documents ClientHandler/ServerHandler</done>
</task>

<task type="auto">
  <name>Task 3: Write deprecation stubs for idb, kit, sanitize, transform</name>
  <files>libraries/idb/README.md, libraries/kit/README.md, libraries/sanitize/README.md, libraries/transform/README.md</files>
  <action>
Write four deprecation stub READMEs following the established Phase 2 nwcache pattern. Each stub MUST include all MD043-required sections (Overview, Installation, Quick Start, Known Limitations, License).

**libraries/idb/README.md:**
1. `# @nostrwatch/idb`
2. `> **DEPRECATED** — This package was not implemented. Use the browser's native IndexedDB API or the `@nostrwatch/route66` cache adapter layer instead.`
3. Overview: Planned IndexedDB wrapper that was never implemented. The directory contains no source code.
4. Installation: N/A — this package is deprecated and contains no code.
5. Quick Start: N/A — see replacement options above.
6. Known Limitations: This package is deprecated and was never implemented. No code exists.
7. License: `[MIT](../../LICENSE)`

**libraries/kit/README.md:**
1. `# @nostrwatch/kit`
2. `> **DEPRECATED** — This package was an empty adapter scaffold that was never completed.`
3. Overview: Planned adapter toolkit that was never implemented beyond an empty directory scaffold.
4. Installation: N/A — this package is deprecated.
5. Quick Start: N/A — this package contains no usable code.
6. Known Limitations: This package is deprecated and contains no implementation.
7. License: `[MIT](../../LICENSE)`

**libraries/sanitize/README.md:**
1. `# @nostrwatch/sanitize`
2. `> **DEPRECATED** — This package has been replaced by [`@nostrwatch/nostrings`](../nostrings/README.md) for relay URL sanitization.`
3. Overview: Legacy relay URL sanitization package, replaced by nostrings.
4. Installation: N/A — this package is deprecated. Use @nostrwatch/nostrings instead.
5. Quick Start: N/A — see @nostrwatch/nostrings.
6. Known Limitations: This package is deprecated. Use @nostrwatch/nostrings for all URL sanitization.
7. License: `[MIT](../../LICENSE)`

**libraries/transform/README.md:**
1. `# @nostrwatch/transform`
2. `> **DEPRECATED** — This package contains empty source files and was never implemented.`
3. Overview: Planned data transformation library that was never implemented. Source files exist but contain no code.
4. Installation: N/A — this package is deprecated.
5. Quick Start: N/A — this package contains no usable code.
6. Known Limitations: This package is deprecated and contains no implementation.
7. License: `[MIT](../../LICENSE)`
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/idb/README.md libraries/kit/README.md libraries/sanitize/README.md libraries/transform/README.md</automated>
  </verify>
  <done>Four deprecation stubs created in libraries/idb/, libraries/kit/, libraries/sanitize/, libraries/transform/. All pass markdownlint-cli2 with all MD043-required sections present.</done>
</task>

</tasks>

<verification>
Run markdownlint-cli2 across all six files:

```sh
cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/worker-relay/README.md libraries/negentropy/README.md libraries/idb/README.md libraries/kit/README.md libraries/sanitize/README.md libraries/transform/README.md
```

All six must pass with zero errors.
</verification>

<success_criteria>
- Six README.md files created
- All pass markdownlint-cli2
- worker-relay README starts with H1 (not H2), documents WorkerRelayInterface
- negentropy README correctly identifies as NIP-77 (not NIP-49), uses pnpm (not yarn)
- Four deprecation stubs include all MD043-required sections
- sanitize stub directs to nostrings as replacement
- Code examples follow styleguide conventions
</success_criteria>

<output>
After completion, create `.planning/phases/03-library-package-readmes/03-06-SUMMARY.md`
</output>
