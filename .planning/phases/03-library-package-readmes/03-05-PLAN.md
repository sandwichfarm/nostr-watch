---
phase: 03-library-package-readmes
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - libraries/db/README.md
  - libraries/websocket/README.md
  - libraries/memory-relay/README.md
autonomous: true
requirements:
  - LIB-09
  - LIB-11
  - LIB-14

must_haves:
  truths:
    - "A developer can read db README and understand the SQLite database abstraction API"
    - "A developer can read websocket README and understand UniversalWebSocket's cross-platform API (Node/Browser/Deno)"
    - "A developer can read memory-relay README and understand the in-memory relay for testing and Svelte integration"
    - "All three READMEs pass pnpm lint:docs without errors"
  artifacts:
    - path: "libraries/db/README.md"
      provides: "Styleguide-conforming README for database client abstractions"
      contains: "## Overview"
    - path: "libraries/websocket/README.md"
      provides: "Styleguide-conforming README for UniversalWebSocket"
      contains: "## Overview"
    - path: "libraries/memory-relay/README.md"
      provides: "Styleguide-conforming README for in-memory Nostr relay"
      contains: "## Overview"
  key_links:
    - from: "libraries/websocket/README.md"
      to: "libraries/nocap/README.md"
      via: "Related Packages links to nocap (uses websocket internally)"
      pattern: "nocap"
---

<objective>
Write styleguide-conforming README.md files for the infrastructure layer libraries: db (database abstractions), websocket (cross-platform WebSocket), and memory-relay (in-memory relay for testing).

Purpose: These three libraries provide foundational infrastructure. websocket is the most complex (full UniversalWebSocket class with cross-platform support). db has an existing README (97 lines, wrong format). memory-relay has no README.
Output: Three README.md files that pass markdownlint-cli2 and render in VitePress.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/styleguide/README.md
@.markdownlint-cli2.jsonc
@.planning/phases/03-library-package-readmes/03-CONTEXT.md
@.planning/phases/03-library-package-readmes/03-RESEARCH.md

<interfaces>
<!-- db: existing README 97 lines wrong format. Package: @nostrwatch/db v0.1.0 -->
<!-- db: SQLite-backed relay database client -->

<!-- websocket: NO README. Package: @nostrwatch/websocket -->
<!-- websocket source: UniversalWebSocket class in src/index.ts -->
<!-- websocket API: on/once/off/connect/send/close/terminate, cross-platform (Node/Browser/Deno) -->

<!-- memory-relay: NO README. Package: @nostrwatch/memory-relay v1.3.0 -->
<!-- memory-relay source: AbstractMemoryRelay class in src/abstract.ts, svelte.ts export -->
<!-- memory-relay: used for testing, Svelte integration -->

<!-- Badge template for published libraries: npm badge, license, status (alpha), runtime -->
<!-- Code style: no semicolons, single quotes, ESM imports, TypeScript -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README for libraries/db</name>
  <files>libraries/db/README.md</files>
  <action>
Read db source:
- `libraries/db/package.json` for metadata (@nostrwatch/db v0.1.0)
- `libraries/db/src/index.ts` for public exports
- Existing README at `libraries/db/README.md` (97 lines) for accurate content to preserve
- Scan source for database client class, query methods, schema

Write `libraries/db/README.md` following the styleguide:

1. **Header**: `# @nostrwatch/db` with npm badge (db), license badge, status badge (alpha), runtime badge (node)
2. **Overview**: SQLite-backed database client for storing and querying relay data. Provides abstracted query methods for relay metadata, check results, and monitoring state. Define relevant terms inline.
3. **Installation**: `pnpm add @nostrwatch/db` + npm alternative
4. **Quick Start**: Show creating a database client and running a basic query. TypeScript, no semicolons.
5. **API**: Document the database client class, query methods, and key types. Preserve accurate API content from existing README.
6. **Known Limitations**: "No known limitations at this time." (no CONCERNS.md entries for db)
7. **Agent Skills**: "No agent skills defined yet for this package."
8. **Related Packages**: Link to libraries/route66 (uses db for state storage), apps/relaymon (uses db for monitoring data), internal/seed (seeds the database)
9. **License**: `[MIT](../../LICENSE)`
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/db/README.md</automated>
  </verify>
  <done>libraries/db/README.md exists, passes markdownlint-cli2, documents database client API, all required sections present</done>
</task>

<task type="auto">
  <name>Task 2: Write README for libraries/websocket</name>
  <files>libraries/websocket/README.md</files>
  <action>
Read websocket source thoroughly:
- `libraries/websocket/package.json` for metadata
- `libraries/websocket/src/index.ts` for UniversalWebSocket class — this is a single-file library with the full implementation

Write `libraries/websocket/README.md` following the styleguide:

1. **Header**: `# @nostrwatch/websocket` with npm badge, license badge, status badge (alpha), runtime badge (node | browser | deno — it's cross-platform)
2. **Overview**: Cross-platform WebSocket client with a unified API for Node.js, browser, and Deno environments. Wraps platform-specific WebSocket implementations behind a common interface with event handling (on/once/off), connection lifecycle management, and automatic environment detection.
3. **Installation**: `pnpm add @nostrwatch/websocket` + npm alternative
4. **Quick Start**: Show the websocket Quick Start from 03-RESEARCH.md code examples — create UniversalWebSocket, register event handlers, connect, send a Nostr REQ message. TypeScript, no semicolons.
5. **API**: Document the UniversalWebSocket class fully:
   - Constructor: `new UniversalWebSocket(url: string, options?)`
   - Event methods: `on(event, handler)`, `once(event, handler)`, `off(event, handler)`
   - Lifecycle: `connect()`, `close()`, `terminate()`
   - Communication: `send(data)`
   - Properties: readyState, url, etc.
   - Events: 'open', 'message', 'close', 'error'
6. **Known Limitations**: "No known limitations at this time." (no CONCERNS.md entries)
7. **Agent Skills**: "No agent skills defined yet for this package."
8. **Related Packages**: Link to libraries/nocap (uses websocket adapter pattern), libraries/route66 (websocket adapter), libraries/memory-relay (in-memory relay for testing)
9. **License**: `[MIT](../../LICENSE)`
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/websocket/README.md</automated>
  </verify>
  <done>libraries/websocket/README.md exists, passes markdownlint-cli2, documents UniversalWebSocket class with all methods and cross-platform support</done>
</task>

<task type="auto">
  <name>Task 3: Write README for libraries/memory-relay</name>
  <files>libraries/memory-relay/README.md</files>
  <action>
Read memory-relay source:
- `libraries/memory-relay/package.json` for metadata (@nostrwatch/memory-relay v1.3.0)
- `libraries/memory-relay/src/abstract.ts` for AbstractMemoryRelay class
- `libraries/memory-relay/src/index.ts` for public exports
- Check for `libraries/memory-relay/src/svelte.ts` for Svelte integration export

Write `libraries/memory-relay/README.md` following the styleguide:

1. **Header**: `# @nostrwatch/memory-relay` with npm badge, license badge, status badge (alpha), runtime badge (browser)
2. **Overview**: In-memory Nostr relay implementation for testing and local development. Provides an AbstractMemoryRelay base class that stores events in memory without persistence. Includes a Svelte integration for use in SvelteKit applications. Define "relay" inline.
3. **Installation**: `pnpm add @nostrwatch/memory-relay` + npm alternative
4. **Quick Start**: Show creating a memory relay instance and using it for testing — storing and querying events. TypeScript, no semicolons.
5. **API**: Document AbstractMemoryRelay class — constructor, event storage methods, query methods, Svelte integration export.
6. **Known Limitations**: "No known limitations at this time." (no CONCERNS.md entries)
7. **Agent Skills**: "No agent skills defined yet for this package."
8. **Related Packages**: Link to libraries/worker-relay (persistent web worker relay), libraries/websocket (WebSocket client), apps/gui (uses memory-relay for local caching)
9. **License**: `[MIT](../../LICENSE)`
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/memory-relay/README.md</automated>
  </verify>
  <done>libraries/memory-relay/README.md exists, passes markdownlint-cli2, documents AbstractMemoryRelay class and Svelte integration</done>
</task>

</tasks>

<verification>
Run markdownlint-cli2 across all three files:

```sh
cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/db/README.md libraries/websocket/README.md libraries/memory-relay/README.md
```

All three must pass with zero errors.
</verification>

<success_criteria>
- Three README.md files created in libraries/db/, libraries/websocket/, libraries/memory-relay/
- All pass markdownlint-cli2
- websocket README documents UniversalWebSocket cross-platform API
- memory-relay README documents AbstractMemoryRelay and Svelte integration
- db README preserves accurate API content from existing README
- Code examples follow styleguide conventions
</success_criteria>

<output>
After completion, create `.planning/phases/03-library-package-readmes/03-05-SUMMARY.md`
</output>
