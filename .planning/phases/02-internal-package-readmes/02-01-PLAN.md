---
phase: 02-internal-package-readmes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/utils/README.md
  - internal/logger/README.md
  - internal/kinds/README.md
autonomous: true
requirements:
  - INT-01
  - INT-03
  - INT-08

must_haves:
  truths:
    - "A developer can read internal/utils/README.md and understand what utility modules are available and how to import them"
    - "A developer can read internal/logger/README.md and understand the child context logging pattern used across the codebase"
    - "A developer can read internal/kinds/README.md and understand that this package exists as a workspace package with no meaningful exports"
    - "All three READMEs pass pnpm lint:docs without errors"
  artifacts:
    - path: "internal/utils/README.md"
      provides: "Styleguide-conforming README for utils package"
      contains: "## Overview"
    - path: "internal/logger/README.md"
      provides: "Styleguide-conforming README for logger package"
      contains: "## Overview"
    - path: "internal/kinds/README.md"
      provides: "Minimal styleguide-conforming README for kinds package"
      contains: "## Overview"
  key_links:
    - from: "internal/utils/README.md"
      to: "docs/styleguide/README.md"
      via: "section order compliance"
      pattern: "## Overview.*## Installation.*## Quick Start.*## Known Limitations.*## License"
    - from: "internal/logger/README.md"
      to: "docs/styleguide/README.md"
      via: "section order compliance"
      pattern: "## Overview.*## Installation.*## Quick Start.*## Known Limitations.*## License"
---

<objective>
Write styleguide-conforming README.md files for the three simplest internal packages: utils, logger, and kinds.

Purpose: These are the most straightforward internal packages — utils is a grab bag of utility modules, logger wraps Pino, and kinds is an empty shell with only build config. Writing these first establishes the README pattern for the rest of the phase.
Output: Three README.md files that pass markdownlint-cli2 and render in VitePress.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/styleguide/README.md
@.markdownlint-cli2.jsonc
@.planning/codebase/CONCERNS.md
@.planning/phases/02-internal-package-readmes/02-CONTEXT.md

<interfaces>
<!-- Styleguide required section order (from .markdownlint-cli2.jsonc MD043): -->
<!-- H1 title, ## Overview, ## Installation, ## Quick Start, ## Known Limitations, ## License -->
<!-- Wildcard (*) between each allows optional sections like Prerequisites, API, Configuration, Agent Skills, Related Packages -->

<!-- Internal package badge pattern (no npm badge): -->
<!-- [![Scope](https://img.shields.io/badge/scope-internal-lightgrey?style=flat-square)] -->
<!-- [![License](https://img.shields.io/github/license/sandwichfarm/nostr-watch?style=flat-square)](LICENSE) -->
<!-- [![Status](https://img.shields.io/badge/status-alpha-orange?style=flat-square)] -->

<!-- Installation pattern for internal packages: -->
<!-- pnpm add @nostrwatch/pkg --filter @nostrwatch/your-package -->

<!-- Code style: no semicolons, single quotes, ESM imports, TypeScript preferred -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README for internal/utils</name>
  <files>internal/utils/README.md</files>
  <action>
Read the utils package source to understand exports:
- `internal/utils/src/index.ts` for the full export list
- `internal/utils/package.json` for metadata
- Scan key modules: `keys.ts`, `array.ts`, `env.ts`, `signing.ts`, `browser.ts`, `class.ts`, `config.ts`, `controlflow.ts`

Write `internal/utils/README.md` following the styleguide:

1. **Header**: `# @nostrwatch/utils` with scope badge (internal, not npm), license badge, status badge (alpha)
2. **Overview**: Shared utility functions used across the monorepo — covers key management, array helpers, environment detection, signing, browser utilities, class helpers, and configuration. Organize the description by mentioning the major module categories (per CONTEXT.md: "organize its many modules into logical subsections").
3. **Installation**: Internal workspace dependency pattern: `pnpm add @nostrwatch/utils --filter @nostrwatch/your-package`. Note it is not published to npm.
4. **Quick Start**: Show 2-3 realistic import examples from different modules (e.g., key utilities, array helpers). Pull actual import patterns from consuming packages in the monorepo. Follow code conventions: no semicolons, single quotes, ESM imports, TypeScript preferred.
5. **API**: Document the 3-5 most important exported modules/functions. Organize by category (keys, arrays, env, etc.). For each, show the function signature and a one-line description. Link to source for the full list. This package has ~15 modules so be selective per CONTEXT.md ("document key exports only, 3-5 most important per package, link to source for the rest").
6. **Known Limitations**: Check CONCERNS.md for utils entries. If none found, write "No known limitations at this time." Do a light scan of the source for TODOs or @ts-nocheck.
7. **Agent Skills**: "No agent skills defined yet for this package."
8. **Related Packages**: Link to internal/logger, internal/controlflow, and any library that heavily consumes utils.
9. **License**: `[MIT](../../LICENSE)`

All section headings must match the MD043 required order. Use real relay URLs where relevant.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 internal/utils/README.md</automated>
  </verify>
  <done>internal/utils/README.md exists, passes markdownlint-cli2, contains Overview with module categories, API section with top exports, and all required styleguide sections in order</done>
</task>

<task type="auto">
  <name>Task 2: Write README for internal/logger</name>
  <files>internal/logger/README.md</files>
  <action>
Read the logger package source:
- `internal/logger/src/index.ts` and `internal/logger/src/logger.ts` for exports and Pino configuration
- `internal/logger/src/browser-logging.ts` for browser support
- `internal/logger/package.json` for metadata

Write `internal/logger/README.md` following the styleguide:

1. **Header**: `# @nostrwatch/logger` with scope badge (internal), license badge, status badge (alpha)
2. **Overview**: Structured logging wrapper built on Pino. Explain that it provides child context logging used throughout the entire codebase (per CONTEXT.md: "show the child context pattern since it's used throughout the entire codebase"). Define Pino briefly if needed.
3. **Installation**: Internal workspace pattern.
4. **Quick Start**: Show the canonical child context pattern: `getLogger().child({ module: 'name' })` (per established pattern from 02-CONTEXT.md). Show a realistic example of how a consuming package creates a child logger and logs with context. Use TypeScript.
5. **API**: Document getLogger, child context creation, log level configuration, and browser logging entry point. Show function signatures.
6. **Configuration**: Document log level configuration (environment variable or programmatic), any Pino options exposed.
7. **Known Limitations**: Check CONCERNS.md — the "Console.log Statements in Production Code" entry mentions `internal/` files. If logger itself has relevant concerns, document them. Otherwise "No known limitations at this time."
8. **Agent Skills**: "No agent skills defined yet for this package."
9. **Related Packages**: Link to packages that consume logger (announce, publisher, seed, etc.)
10. **License**: `[MIT](../../LICENSE)`
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 internal/logger/README.md</automated>
  </verify>
  <done>internal/logger/README.md exists, passes markdownlint-cli2, shows child context logging pattern in Quick Start, and all required styleguide sections in order</done>
</task>

<task type="auto">
  <name>Task 3: Write minimal README for internal/kinds</name>
  <files>internal/kinds/README.md</files>
  <action>
Read the kinds package:
- `internal/kinds/` has only `rollup.config.json` and `tsconfig.json` — no package.json, no src/, no meaningful exports
- Per CONTEXT.md decision: "if source directory is empty or has no meaningful exports, write a minimal README (overview + installation + license) rather than a deprecation stub, since they're still workspace packages"

Write `internal/kinds/README.md` as a minimal README:

1. **Header**: `# @nostrwatch/kinds` with scope badge (internal), license badge, status badge (alpha)
2. **Overview**: Brief explanation that this package exists as a workspace placeholder for Nostr event kind constants. Note that it currently has no meaningful exports — it contains only build configuration (rollup.config.json, tsconfig.json). Mention it is retained as a workspace package.
3. **Installation**: Internal workspace pattern. Note it has no package.json and no publishable exports currently.
4. **Quick Start**: Keep minimal — note there are no exports to demonstrate. A one-line note is sufficient.
5. **Known Limitations**: "This package has no active code or exports. It exists as a workspace placeholder."
6. **License**: `[MIT](../../LICENSE)`

IMPORTANT: Even though this is minimal, it MUST still include all MD043-required sections (Overview, Installation, Quick Start, Known Limitations, License) to pass linting. The content can be brief but sections must exist.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 internal/kinds/README.md</automated>
  </verify>
  <done>internal/kinds/README.md exists, passes markdownlint-cli2, accurately describes kinds as a minimal workspace package with no active exports</done>
</task>

</tasks>

<verification>
Run markdownlint-cli2 across all three files:

```sh
cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 internal/utils/README.md internal/logger/README.md internal/kinds/README.md
```

All three must pass with zero errors.
</verification>

<success_criteria>
- Three README.md files created in internal/utils/, internal/logger/, internal/kinds/
- All pass markdownlint-cli2 (MD043 section order, MD040 language tags, etc.)
- utils README organizes modules into logical categories with top 3-5 exports documented
- logger README demonstrates child context logging pattern
- kinds README is minimal but includes all required sections
- Code examples follow styleguide conventions (no semicolons, single quotes, ESM imports, TypeScript)
</success_criteria>

<output>
After completion, create `.planning/phases/02-internal-package-readmes/02-01-SUMMARY.md`
</output>
