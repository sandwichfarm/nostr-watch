---
phase: 02-internal-package-readmes
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/publisher/README.md
  - internal/controlflow/README.md
  - internal/announce/README.md
autonomous: true
requirements:
  - INT-02
  - INT-04
  - INT-07

must_haves:
  truths:
    - "A developer can read internal/publisher/README.md and understand the adapter pattern, what kinds of events it publishes, and how to use it"
    - "A developer can read internal/controlflow/README.md and understand what control flow utilities are available (queues, retry logic)"
    - "A developer can read internal/announce/README.md and understand the NIP-66 announcement system and when it runs"
    - "publisher README documents the known language tag validation limitation from CONCERNS.md"
    - "All three READMEs pass pnpm lint:docs without errors"
  artifacts:
    - path: "internal/publisher/README.md"
      provides: "Styleguide-conforming README documenting adapter pattern"
      contains: "## Overview"
    - path: "internal/controlflow/README.md"
      provides: "Styleguide-conforming README for control flow utilities"
      contains: "## Overview"
    - path: "internal/announce/README.md"
      provides: "Styleguide-conforming README for announcement system"
      contains: "## Overview"
  key_links:
    - from: "internal/publisher/README.md"
      to: ".planning/codebase/CONCERNS.md"
      via: "Known Limitations section references language tag validation concern"
      pattern: "language tag"
---

<objective>
Write styleguide-conforming README.md files for the three behavioral internal packages: publisher, controlflow, and announce.

Purpose: These packages implement key behavioral patterns — publisher has the adapter pattern (a core architectural concept in the monorepo), announce handles NIP-66 boot announcements, and controlflow provides queue and retry utilities. Publisher needs the most thorough documentation of the three.
Output: Three README.md files that pass markdownlint-cli2 and render in VitePress.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/styleguide/README.md
@.markdownlint-cli2.jsonc
@.planning/codebase/CONCERNS.md
@.planning/phases/02-internal-package-readmes/02-CONTEXT.md

<interfaces>
<!-- Internal package badge pattern (no npm badge): -->
<!-- [![Scope](https://img.shields.io/badge/scope-internal-lightgrey?style=flat-square)] -->

<!-- Installation: pnpm add @nostrwatch/pkg --filter @nostrwatch/your-package -->

<!-- MD043 required sections: Overview, Installation, Quick Start, Known Limitations, License -->

<!-- CONCERNS.md relevant entry for publisher: -->
<!-- "Unfinished Language Tag Validation" in internal/publisher/src/kinds/Kind30166.ts line 156 -->
<!-- "Console.log Statements in Production Code" mentions internal/announce/ and internal/publisher/ -->

<!-- Code style: no semicolons, single quotes, ESM imports, TypeScript preferred -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README for internal/publisher</name>
  <files>internal/publisher/README.md</files>
  <action>
Read the publisher package source thoroughly — this is the most complex internal package:
- `internal/publisher/src/index.ts` for exports
- `internal/publisher/src/Publisher.ts` for the main Publisher class
- `internal/publisher/src/Event.ts` for event construction
- `internal/publisher/src/kinds/` directory for supported event kinds (Kind30166.ts etc.)
- `internal/publisher/src/builders/` for event builder pattern
- `internal/publisher/package.json` for metadata and dependencies

Write `internal/publisher/README.md` following the styleguide:

1. **Header**: `# @nostrwatch/publisher` with scope badge, license badge, status badge (alpha)
2. **Overview**: Library for publishing nostr.watch relay status events. Explain the adapter pattern clearly (per CONTEXT.md: "document the adapter pattern clearly since it's a key architectural concept"). Mention it publishes NIP-66 kind 30166 events and related event types. Define NIP-66 inline on first use.
3. **Installation**: Internal workspace pattern.
4. **Quick Start**: Show realistic usage — how to create a Publisher instance, configure it with a signing key, and publish a relay status event. Pull actual usage patterns from internal/announce (per CONTEXT.md: "how publisher is used in announce"). Use TypeScript, no semicolons.
5. **API**: Document the Publisher class (constructor, publish method, configuration), Event class, supported event kinds (Kind30166 etc.), and the adapter interface (NostrTools adapter). This package warrants more thorough API coverage per CONTEXT.md ("Packages with rich exports get more thorough API coverage"). Show the adapter registration pattern.
6. **Configuration**: Document any publisher configuration options, signing key setup.
7. **Known Limitations**: MUST include the language tag validation concern from CONCERNS.md: "Unfinished Language Tag Validation — Language tag validation in Kind30166.ts is not implemented. Invalid ISO-639-1 language tags may be published in NIP-30166 events without validation." Also note console.log statements in production code if found in source scan. Follow the Known Limitations template format from styleguide.
8. **Agent Skills**: "No agent skills defined yet for this package."
9. **Related Packages**: Link to internal/announce (primary consumer), libraries/nip66 (protocol spec), libraries/nocap (related adapter pattern).
10. **License**: `[MIT](../../LICENSE)`
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 internal/publisher/README.md</automated>
  </verify>
  <done>internal/publisher/README.md exists, passes markdownlint-cli2, documents adapter pattern, includes Known Limitations with language tag validation concern, and all required sections in order</done>
</task>

<task type="auto">
  <name>Task 2: Write README for internal/controlflow</name>
  <files>internal/controlflow/README.md</files>
  <action>
Read the controlflow package source:
- `internal/controlflow/src/index.ts` for exports
- `internal/controlflow/src/queues.ts` for queue utilities
- `internal/controlflow/src/retry.ts` for retry logic
- `internal/controlflow/package.json` for metadata

Write `internal/controlflow/README.md` following the styleguide:

1. **Header**: `# @nostrwatch/controlflow` with scope badge, license badge, status badge (alpha)
2. **Overview**: Provides queue management and retry utilities for application control flow across the monorepo. Explain what problems these solve (rate limiting, retry with backoff, task queuing).
3. **Installation**: Internal workspace pattern.
4. **Quick Start**: Show a realistic example of using the queue or retry utility. Pull actual usage from consuming packages if visible.
5. **API**: Document exported queue functions and retry utilities with signatures.
6. **Known Limitations**: Check CONCERNS.md and scan source for TODOs. If none relevant, "No known limitations at this time."
7. **Agent Skills**: "No agent skills defined yet for this package."
8. **Related Packages**: Link to packages that consume controlflow utilities.
9. **License**: `[MIT](../../LICENSE)`
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 internal/controlflow/README.md</automated>
  </verify>
  <done>internal/controlflow/README.md exists, passes markdownlint-cli2, documents queue and retry exports, all required sections in order</done>
</task>

<task type="auto">
  <name>Task 3: Write README for internal/announce</name>
  <files>internal/announce/README.md</files>
  <action>
Read the announce package source:
- `internal/announce/src/index.ts` for exports and main logic
- `internal/announce/package.json` for metadata and description ("Generates a NIP-66 10166 event, NIP-65 10002 event and NIP-01 0 events for monitors on every boot")

Write `internal/announce/README.md` following the styleguide:

1. **Header**: `# @nostrwatch/announce` with scope badge, license badge, status badge (alpha)
2. **Overview**: Generates NIP-66 10166, NIP-65 10002, and NIP-01 kind 0 announcement events for monitors on every boot. Define NIP-66 and NIP-65 inline on first use. Explain the "announce on boot" pattern and why monitors need to announce themselves.
3. **Installation**: Internal workspace pattern.
4. **Quick Start**: Show how announce is typically invoked — likely called during app bootstrap. Show the function call pattern with realistic parameters.
5. **API**: Document the main exported function(s), parameters, and return types.
6. **Known Limitations**: CONCERNS.md mentions "Console.log Statements in Production Code" for internal/announce/. If console.log is found in source, note it. Otherwise "No known limitations at this time."
7. **Agent Skills**: "No agent skills defined yet for this package."
8. **Related Packages**: Link to internal/publisher (used for event publishing), libraries/nip66 (NIP-66 protocol), apps/relaymon (primary consumer).
9. **License**: `[MIT](../../LICENSE)`
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 internal/announce/README.md</automated>
  </verify>
  <done>internal/announce/README.md exists, passes markdownlint-cli2, explains NIP-66 announcement pattern, all required sections in order</done>
</task>

</tasks>

<verification>
Run markdownlint-cli2 across all three files:

```sh
cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 internal/publisher/README.md internal/controlflow/README.md internal/announce/README.md
```

All three must pass with zero errors.
</verification>

<success_criteria>
- Three README.md files created in internal/publisher/, internal/controlflow/, internal/announce/
- All pass markdownlint-cli2
- publisher README documents adapter pattern clearly and includes Known Limitations for language tag validation
- controlflow README documents queue and retry utilities
- announce README explains NIP-66/NIP-65/NIP-01 announcement events on boot
- Code examples follow styleguide conventions
</success_criteria>

<output>
After completion, create `.planning/phases/02-internal-package-readmes/02-02-SUMMARY.md`
</output>
