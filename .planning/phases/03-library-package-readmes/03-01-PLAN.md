---
phase: 03-library-package-readmes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - libraries/nostrings/README.md
  - libraries/nip66/README.md
  - libraries/schemata/README.md
  - libraries/schemata-js-ajv/README.md
autonomous: true
requirements:
  - LIB-13
  - LIB-12
  - LIB-04
  - LIB-05

must_haves:
  truths:
    - "A developer can read nostrings README and understand the four URL operations (sanitize, qualify, normalize, dedup) without reading source"
    - "A developer unfamiliar with NIP-66 can read the nip66 README and understand event kinds 10166, 30166, and 1066 and their data model"
    - "A developer can see schemata is migrating to @nostrability/schemata and knows where to find the replacement"
    - "A developer can read schemata-js-ajv README and understand it wraps schemata schemas with AJV validation"
    - "All four READMEs pass pnpm lint:docs without errors"
  artifacts:
    - path: "libraries/nostrings/README.md"
      provides: "Styleguide-conforming README for relay URL validation"
      contains: "## Overview"
    - path: "libraries/nip66/README.md"
      provides: "Protocol documentation README for NIP-66 event kinds and data model"
      contains: "## Overview"
    - path: "libraries/schemata/README.md"
      provides: "Styleguide-conforming README noting migration to @nostrability/schemata"
      contains: "## Overview"
    - path: "libraries/schemata-js-ajv/README.md"
      provides: "Styleguide-conforming README for AJV validation wrapper"
      contains: "## Overview"
  key_links:
    - from: "libraries/schemata-js-ajv/README.md"
      to: "libraries/schemata/README.md"
      via: "Related Packages cross-link"
      pattern: "schemata"
    - from: "libraries/nip66/README.md"
      to: "internal/publisher/README.md"
      via: "Related Packages links to implementation"
      pattern: "publisher"
---

<objective>
Write styleguide-conforming README.md files for the protocol and validation layer libraries: nostrings (relay URL validation), nip66 (NIP-66 protocol documentation), schemata (JSON Schema definitions), and schemata-js-ajv (AJV validation wrapper).

Purpose: These four libraries form the protocol/validation foundation. nostrings handles relay URL cleaning, nip66 documents the NIP-66 protocol used throughout the monorepo, and the schemata pair provides JSON Schema validation. The nip66 README is a phase success criterion — it must explain the protocol clearly enough for NIP-66 newcomers.
Output: Four README.md files that pass markdownlint-cli2 and render in VitePress.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/styleguide/README.md
@.markdownlint-cli2.jsonc
@.planning/codebase/CONCERNS.md
@.planning/phases/03-library-package-readmes/03-CONTEXT.md
@.planning/phases/03-library-package-readmes/03-RESEARCH.md

<interfaces>
<!-- Library README pattern: npm badge (not scope badge), pnpm add install, full API section required -->

<!-- Badge template for published libraries: -->
<!-- [![npm version](https://img.shields.io/npm/v/@nostrwatch/PACKAGE-NAME?style=flat-square&label=npm)](https://www.npmjs.com/package/@nostrwatch/PACKAGE-NAME) -->
<!-- [![License](https://img.shields.io/github/license/sandwichfarm/nostr-watch?style=flat-square)](LICENSE) -->
<!-- [![Status](https://img.shields.io/badge/status-alpha-orange?style=flat-square)](https://github.com/sandwichfarm/nostr-watch) -->
<!-- [![Runtime](https://img.shields.io/badge/runtime-RUNTIMES-blue?style=flat-square)](https://github.com/sandwichfarm/nostr-watch) -->

<!-- MD043 required sections: Overview, Installation, Quick Start, Known Limitations, License -->
<!-- Additional required library sections per styleguide: API, Agent Skills, Related Packages -->

<!-- Code style: no semicolons, single quotes, ESM imports, TypeScript, ts language tag -->
<!-- Installation: pnpm add @nostrwatch/pkg (published to npm) -->
<!-- Relay URLs: wss://relay.damus.io, wss://nos.lol -->

<!-- nostrings: existing README 110 lines, wrong package name (@nostrwatch/relay-sanitizer). Actual package: @nostrwatch/nostrings v0.3.0 -->
<!-- nostrings exports: sanitize, qualify, normalize, dedup -->

<!-- nip66: NO source, NO package.json. Write as protocol documentation library. -->
<!-- NIP-66 event kinds: 10166 (monitor announcement), 30166 (relay status), 1066 (relay status delta) -->
<!-- Source NIP-66 data from internal/publisher/src/kinds/ and libraries/nocap-route66/ -->

<!-- schemata: existing README 52 lines wrong format. Package migrating to @nostrability/schemata -->
<!-- schemata-js-ajv: existing README 60 lines wrong format. Uses @nostrability/schemata, exports validateNip11/validateMessage/validateNote -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write README for libraries/nostrings</name>
  <files>libraries/nostrings/README.md</files>
  <action>
Read source files to verify API:
- `libraries/nostrings/package.json` for package name (@nostrwatch/nostrings), version, description
- `libraries/nostrings/src/index.ts` for public exports (sanitize, qualify, normalize, dedup)
- Existing README at `libraries/nostrings/README.md` for any accurate content to preserve

Write `libraries/nostrings/README.md` following the styleguide:

1. **Header**: `# @nostrwatch/nostrings` with npm badge (nostrings), license badge, status badge (alpha), runtime badge (node | browser)
2. **Overview**: Relay URL validation and normalization utility. Exports functions to sanitize (clean invalid characters), qualify (validate wss:// protocol), normalize (canonical form), and dedup (remove duplicates from arrays). Define "relay" inline on first use.
3. **Installation**: `pnpm add @nostrwatch/nostrings` + npm alternative
4. **Quick Start**: Show sanitize + normalize pipeline on a messy relay URL, producing clean output. TypeScript, no semicolons, single quotes.
5. **API**: Document each exported function — sanitize, qualify, normalize, dedup — with signatures, parameter descriptions, return types. Read actual source for accurate signatures.
6. **Known Limitations**: "No known limitations at this time." (no CONCERNS.md entries for nostrings)
7. **Agent Skills**: "No agent skills defined yet for this package."
8. **Related Packages**: Link to libraries/nocap (uses nostrings for URL validation), libraries/route66 (relay URL handling)
9. **License**: `[MIT](../../LICENSE)`

CRITICAL: Use @nostrwatch/nostrings as the package name everywhere — NOT the old name @nostrwatch/relay-sanitizer.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/nostrings/README.md</automated>
  </verify>
  <done>libraries/nostrings/README.md exists, passes markdownlint-cli2, uses correct package name @nostrwatch/nostrings, documents all four exported functions</done>
</task>

<task type="auto">
  <name>Task 2: Write README for libraries/nip66</name>
  <files>libraries/nip66/README.md</files>
  <action>
This directory has NO source and NO package.json — it exists as a requirement target (LIB-12). Write it as protocol documentation that explains the NIP-66 event model used throughout the monorepo.

Source NIP-66 protocol data from:
- `internal/publisher/src/kinds/` — Kind10166.ts, Kind30166.ts for event structure
- `libraries/nocap-route66/src/kinds/` — 30166.ts, 10166.ts, 0.ts for tag structure
- The NIP-66 reference table in 03-RESEARCH.md

Write `libraries/nip66/README.md` following the styleguide:

1. **Header**: `# @nostrwatch/nip66` — NO npm badge (not published). Use scope badge (internal-style) since it has no package.json. License badge, status badge (alpha).
2. **Overview**: Protocol documentation for NIP-66 (Relay Monitoring System). NIP-66 defines event kinds for relay health monitoring on the Nostr network. This package serves as the reference documentation for the event kinds, tag schemas, and data model used by `@nostrwatch/publisher` and `@nostrwatch/nocap-route66`. Link to the [NIP-66 spec](https://github.com/nostr-protocol/nips/blob/master/66.md). Define NIP, relay, event inline on first use.
3. **Installation**: State that this package has no installable code — it is protocol documentation. Reference `@nostrwatch/publisher` for the implementation.
4. **Quick Start**: Show what NIP-66 events look like — a kind 30166 event JSON example with key tags (d, r, n, R, rtt-open, nip11, ssl, dns). Show a kind 10166 monitor announcement event. These are data examples, not runnable code.
5. **API — Event Kinds**: Document the three event kinds in a table:
   - Kind 10166 (Replaceable): Monitor announcement — declares capabilities, check frequency, networks monitored
   - Kind 30166 (Parameterized replaceable): Relay status — one event per relay per monitor, d-tag = relay URL
   - Kind 1066 (Regular): Relay status delta — append-only history for relay-chronicle
   Document key tags for each kind in tables (from 03-RESEARCH.md NIP-66 Protocol Reference section).
6. **Known Limitations**: "This package contains protocol documentation only, with no executable code. For the implementation, see `@nostrwatch/publisher`."
7. **Agent Skills**: "No agent skills defined yet for this package."
8. **Related Packages**: Link to internal/publisher (implementation), libraries/nocap-route66 (event builders), libraries/nocap (data source for relay checks), libraries/relay-chronicle (consumes kind 1066 events)
9. **License**: `[MIT](../../LICENSE)`

This README is a phase success criterion — a developer unfamiliar with NIP-66 must understand the protocol's event kinds and data model after reading it.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/nip66/README.md</automated>
  </verify>
  <done>libraries/nip66/README.md exists, passes markdownlint-cli2, explains all three NIP-66 event kinds (10166, 30166, 1066) with tag tables, a developer unfamiliar with NIP-66 can understand the protocol from this README alone</done>
</task>

<task type="auto">
  <name>Task 3: Write READMEs for libraries/schemata and libraries/schemata-js-ajv</name>
  <files>libraries/schemata/README.md, libraries/schemata-js-ajv/README.md</files>
  <action>
Read source for both packages:
- `libraries/schemata/package.json` for name (@nostrwatch/schemata), version, description
- `libraries/schemata/src/index.ts` for exports
- `libraries/schemata/README.md` (existing 52 lines) for accurate content
- `libraries/schemata-js-ajv/package.json` for name, version
- `libraries/schemata-js-ajv/src/index.ts` for exports (validateNip11, validateMessage, validateNote)
- `libraries/schemata-js-ajv/README.md` (existing 60 lines) for accurate content

Write `libraries/schemata/README.md`:

1. **Header**: `# @nostrwatch/schemata` with npm badge, license badge, status badge (alpha)
2. **Overview**: JSON Schema definitions for Nostr protocol data structures. Note prominently that this package is migrating to `@nostrability/schemata` (separate npm org). Define "schema" and relevant Nostr terms inline.
3. **Installation**: `pnpm add @nostrwatch/schemata` + npm. Note migration.
4. **Quick Start**: Show importing and using a schema definition. TypeScript.
5. **API**: Document exported schemas based on actual source.
6. **Known Limitations**: Note the in-progress migration to @nostrability/schemata.
7. **Agent Skills**: "No agent skills defined yet for this package."
8. **Related Packages**: Link to schemata-js-ajv (AJV validation companion), libraries/auditor (uses schemas for validation)
9. **License**: `[MIT](../../LICENSE)`

Write `libraries/schemata-js-ajv/README.md`:

1. **Header**: `# @nostrwatch/schemata-js-ajv` with npm badge, license badge, status badge (alpha), runtime badge
2. **Overview**: AJV-powered validation using @nostrwatch/schemata (or @nostrability/schemata) JSON schemas. Provides ready-to-use validation functions for NIP-11 info documents, Nostr messages, and notes.
3. **Installation**: `pnpm add @nostrwatch/schemata-js-ajv` + npm
4. **Quick Start**: Show validateNip11() with a realistic NIP-11 document. TypeScript.
5. **API**: Document validateNip11, validateMessage, validateNote with signatures and return types.
6. **Known Limitations**: "No known limitations at this time." (no CONCERNS.md entries)
7. **Agent Skills**: "No agent skills defined yet for this package."
8. **Related Packages**: Link to schemata (schema source), libraries/nocap (uses NIP-11 validation)
9. **License**: `[MIT](../../LICENSE)`

Cross-link the two packages heavily in Related Packages sections.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/schemata/README.md libraries/schemata-js-ajv/README.md</automated>
  </verify>
  <done>Both libraries/schemata/README.md and libraries/schemata-js-ajv/README.md exist, pass markdownlint-cli2, cross-link each other, schemata notes migration to @nostrability/schemata</done>
</task>

</tasks>

<verification>
Run markdownlint-cli2 across all four files:

```sh
cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/nostrings/README.md libraries/nip66/README.md libraries/schemata/README.md libraries/schemata-js-ajv/README.md
```

All four must pass with zero errors.
</verification>

<success_criteria>
- Four README.md files created in libraries/nostrings/, libraries/nip66/, libraries/schemata/, libraries/schemata-js-ajv/
- All pass markdownlint-cli2
- nostrings uses correct package name @nostrwatch/nostrings (not old @nostrwatch/relay-sanitizer)
- nip66 README explains NIP-66 event kinds and data model clearly for newcomers (phase success criterion)
- schemata notes migration to @nostrability/schemata
- schemata and schemata-js-ajv cross-link in Related Packages
- Code examples follow styleguide conventions (TypeScript, no semicolons, single quotes, ESM imports)
</success_criteria>

<output>
After completion, create `.planning/phases/03-library-package-readmes/03-01-SUMMARY.md`
</output>
