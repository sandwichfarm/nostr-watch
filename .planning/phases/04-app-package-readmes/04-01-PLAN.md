---
phase: 04-app-package-readmes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - apps/gui/README.md
  - apps/rstate/README.md
autonomous: true
requirements:
  - APP-01
  - APP-02

must_haves:
  truths:
    - "A developer reading apps/gui/README.md understands it is a SvelteKit relay dashboard, how to run it locally with pnpm dev, and what workspace packages it depends on"
    - "A developer reading apps/rstate/README.md understands it is a ContextVM relay state machine with REST API, how to configure and start it, and what env vars are required"
    - "Both READMEs pass markdownlint-cli2 (MD043 section order) without modification"
    - "gui README surfaces all 3 CONCERNS.md entries (table config separation, worker fallback, store race conditions) in Known Limitations"
    - "rstate README surfaces all 3 CONCERNS.md entries (SDK stubs, dev-tools @ts-nocheck, console.log in scoring) in Known Limitations"
  artifacts:
    - path: "apps/gui/README.md"
      provides: "SvelteKit dashboard README with Prerequisites, Quick Start, Known Limitations"
      contains: "## Overview"
    - path: "apps/rstate/README.md"
      provides: "ContextVM relay state machine README with REST API docs, env var table, Known Limitations"
      contains: "## Overview"
  key_links:
    - from: "apps/gui/README.md"
      to: "docs/styleguide/README.md"
      via: "MD043 heading order compliance"
      pattern: "## Overview.*## Installation.*## Quick Start.*## Known Limitations.*## License"
    - from: "apps/rstate/README.md"
      to: "docs/styleguide/README.md"
      via: "MD043 heading order compliance"
      pattern: "## Overview.*## Installation.*## Quick Start.*## Known Limitations.*## License"
---

<objective>
Write conforming README.md files for the two most complex app packages: apps/gui (SvelteKit relay dashboard) and apps/rstate (ContextVM relay state machine with REST API).

Purpose: These are the most feature-rich apps in the monorepo and require the most detailed documentation -- gui is a full SvelteKit 2.5 dashboard with multiple workspace dependencies, and rstate exposes a Fastify REST API with YAML+env configuration.
Output: Two styleguide-conforming README.md files that pass MD043 linting.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-app-package-readmes/04-RESEARCH.md
@.planning/codebase/CONCERNS.md
@docs/styleguide/README.md
@.markdownlint-cli2.jsonc
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write apps/gui README.md</name>
  <files>apps/gui/README.md</files>
  <action>
Write a complete styleguide-conforming README for apps/gui. The existing README has useful content under wrong section names -- extract technical accuracy from it, discard non-conforming structure, and rewrite to match MD043.

**Package details (confirmed from package.json and source):**
- npm name: `@nostrwatch/gui`, version 0.6.77
- Tech: SvelteKit 2.5, Svelte 5, Vite 5, TailwindCSS 3.4, TypeScript 5.7
- Runtime: browser only (SSR disabled, static adapter)
- Status badge: alpha
- Runtime badge: browser

**Required MD043 section order:** H1, ## Overview, ## Installation, ## Quick Start, ## Known Limitations, ## License

**Section content:**

H1: `# @nostrwatch/gui`
- Tagline: "SvelteKit dashboard for monitoring and exploring Nostr relays."
- Badges: npm version (use `@nostrwatch/gui`), License MIT, Status alpha, Runtime browser

## Overview (2-4 sentences): Web-based dashboard for relay monitoring. Built with SvelteKit 2.5 and Svelte 5. Uses IndexedDB (Dexie) for client-side storage. Connects to the nostr-watch relay network for real-time relay data.

## Prerequisites: Node.js >=22 (Volta pinned to 22.15.0), pnpm >=8. No environment variables needed -- gui is a client-only app with runtime config via in-app Preferences.

## Installation:
```sh
# From monorepo root
pnpm install
```

## Quick Start:
```sh
# Start development server (port 5173)
pnpm --filter @nostrwatch/gui dev

# Production build
pnpm --filter @nostrwatch/gui build

# Preview built bundle
pnpm --filter @nostrwatch/gui preview
```

## Configuration: No env vars. Runtime configuration available through the in-app Preferences page (cache wipe, monitor management). Vite config at `vite.config.js`.

## Known Limitations (MUST include all 3 CONCERNS.md entries):
1. **GUI Table Configuration Separation** -- User config not separated from built-in config in `table/utils.ts`; no clean override mechanism for table column settings.
2. **Worker-based Computation Fallback Issues** -- `dimensions-worker-manager.ts` falls back silently to legacy stores when workers fail; no telemetry tracks fallback frequency.
3. **GUI Store Initialization Race Conditions** -- Multiple stores in `seed.ts` and `nip11s.ts` depend on StateManager initialization timing; race conditions possible on slow hardware.

## Agent Skills: "No skills defined yet" placeholder.

## Related Packages: Cross-link to route66, nocap, worker-relay, relay-charts, relay-chronicle, utils (all confirmed workspace dependencies from package.json).

## License: `[MIT](../../LICENSE)`

**Anti-patterns to avoid:**
- Do NOT use semicolons in any TypeScript examples
- Do NOT fabricate environment variables (gui has none)
- Verify badge URL uses correct npm package name `@nostrwatch/gui`
- Ensure blank lines around all headings (MD022)
- Every fenced code block needs a language tag (MD040)
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && pnpm lint:docs 2>&1 | grep -E "gui|error|warning" || echo "lint passed"</automated>
  </verify>
  <done>apps/gui/README.md exists, passes markdownlint-cli2 MD043, contains all 3 CONCERNS.md Known Limitations entries, has Prerequisites/Quick Start/Configuration sections appropriate for a browser SvelteKit app</done>
</task>

<task type="auto">
  <name>Task 2: Write apps/rstate README.md</name>
  <files>apps/rstate/README.md</files>
  <action>
Write a complete styleguide-conforming README for apps/rstate. The existing README (apps/rstate/README.md) is comprehensive but non-conforming -- it has valuable REST API docs and config reference that should be restructured, not rewritten.

**Package details (confirmed from package.json and source):**
- npm name: `@nostr-watch/rstate` (NOTE: hyphenated scope -- NOT `@nostrwatch/rstate`)
- Version: 0.1.0
- Tech: Node.js, Fastify 5.7, ContextVM SDK, TypeScript 5.7, tsup
- Runtime: node
- Status badge: alpha

**CRITICAL: Package name uses `@nostr-watch/rstate` (hyphen in scope), not `@nostrwatch/rstate`. Badge URL must reflect this.**

**Required MD043 section order:** H1, ## Overview, ## Installation, ## Quick Start, ## Known Limitations, ## License

**Section content:**

H1: `# @nostr-watch/rstate`
- Tagline: "ContextVM relay state machine with REST API for aggregated relay intelligence."
- Badges: npm version (use `@nostr-watch/rstate` -- hyphenated!), License MIT, Status alpha, Runtime node

## Overview (3-5 sentences): Relay state aggregation engine. Ingests NIP-66 relay check events, computes scores, and serves aggregated relay intelligence via REST API. Built on ContextVM SDK for decentralized computation with MCP-over-Nostr integration. Exposes OpenAPI docs at `/docs`.

## Prerequisites: Node.js >=20. Environment variable table (use env var table format from styleguide):

| Variable | Required | Description | Example |
|----------|----------|-------------|---------|
| `CVM_RELAYS` | Yes (if CVM enabled) | Comma-separated relay URLs for CVM transport | `wss://relay.damus.io` |
| `INGEST_RELAYS` | Yes | Comma-separated relay URLs for NIP-66 ingestion | `wss://history.nostr.watch` |
| `CVM_SERVER_NSEC` | Yes (if CVM enabled) | Server private key for signing | `nsec1...` |
| `REST_ENABLED` | No | Enable REST API | `true` |
| `REST_PORT` | No | REST API port | `3000` |
| `LOG_LEVEL` | No | Logging verbosity | `info` |

## Installation:
```sh
# From monorepo root
pnpm install

# Or from app directory
cd apps/rstate
npm install
```

## Quick Start:
```sh
cp config.sample.yaml config.yaml
# Edit config.yaml with relay URLs and keys
npm run build
npm start
```

## API: Document the REST API endpoint groups in a table. Key groups from existing README: health check, relay listing/state/search/nearby/labels/by-software/network/nip/country/compare/online/offline/dead, monitors, policy, subscriptions. Include SSE endpoint for real-time updates. Note OpenAPI docs available at `/docs`.

Use a table format with columns: Method, Endpoint, Description. Cover the most important endpoints (8-12 rows covering the main groups). Do NOT duplicate the full 50+ endpoint list from the existing README -- summarize key groups.

## Configuration: YAML primary, env var override. Config file path set via `CONFIG_FILE` env var or `--config` flag. Document key config sections: server (REST port, host), cvm (relays, nsec), ingest (relay URLs), cache, aggregation, logging. Reference `config.sample.yaml` for full example.

## Known Limitations (MUST include all 3 CONCERNS.md entries):
1. **SDK Stub Dependencies** -- `sdk-stubs.ts` contains MockRelayPool and MockSigner with fake signatures; production use requires replacing with real ContextVM SDK classes.
2. **Outdated Development Utilities** -- `dev-tools.ts` uses `@ts-nocheck` with old RelayState structure; dev utilities are unreliable for debugging current data shapes.
3. **Console.log in Scoring** -- `monitor-scoring.ts` has unconditional `console.log` at lines 38, 47, 52; these will spam production logs.

## Agent Skills: "No skills defined yet" placeholder.

## Related Packages: Cross-link to internal/publisher, internal/logger, internal/utils.

## License: `[MIT](../../LICENSE)`

**Anti-patterns to avoid:**
- Do NOT use `@nostrwatch/rstate` -- the package name is `@nostr-watch/rstate` (hyphenated scope)
- Do NOT copy the full 50+ endpoint table from existing README -- summarize into key groups
- Do NOT fabricate env vars beyond those confirmed in source
- Ensure blank lines around all headings (MD022)
- Every fenced code block needs a language tag (MD040)
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && pnpm lint:docs 2>&1 | grep -E "rstate|error|warning" || echo "lint passed"</automated>
  </verify>
  <done>apps/rstate/README.md exists, passes markdownlint-cli2 MD043, contains all 3 CONCERNS.md Known Limitations entries, has env var table in Prerequisites, summarized REST API table, and Configuration section documenting YAML+env approach</done>
</task>

</tasks>

<verification>
Run `pnpm lint:docs` from monorepo root. Both apps/gui/README.md and apps/rstate/README.md must pass with zero errors. Verify each README contains: ## Overview, ## Installation, ## Quick Start, ## Known Limitations, ## License in order.
</verification>

<success_criteria>
- apps/gui/README.md and apps/rstate/README.md both exist and pass `pnpm lint:docs`
- gui README has 3 Known Limitations from CONCERNS.md
- rstate README has 3 Known Limitations from CONCERNS.md
- rstate README uses correct hyphenated package name `@nostr-watch/rstate`
- rstate README has env var table and REST API summary
- Both READMEs follow styleguide badge, code example, and tone conventions
</success_criteria>

<output>
After completion, create `.planning/phases/04-app-package-readmes/04-01-SUMMARY.md`
</output>
