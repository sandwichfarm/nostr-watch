---
phase: 03-library-package-readmes
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - libraries/nostrawl/README.md
  - libraries/relay-charts/README.md
  - libraries/relay-chronicle/README.md
  - libraries/uptime-kuma-monitor/README.md
autonomous: true
requirements:
  - LIB-08
  - LIB-06
  - LIB-07
  - LIB-17

must_haves:
  truths:
    - "A developer can read nostrawl README and understand the queue-based web crawler with PQueue/BullMQ adapters"
    - "A developer can read relay-charts README and understand the Chart.js/ECharts/Recharts adapter system for relay metrics"
    - "A developer can read relay-chronicle README and understand historical relay event management"
    - "A developer can read uptime-kuma-monitor README and understand both the CLI and library API"
    - "All four READMEs pass pnpm lint:docs without errors"
  artifacts:
    - path: "libraries/nostrawl/README.md"
      provides: "Styleguide-conforming README for queue-based web crawler"
      contains: "## Overview"
    - path: "libraries/relay-charts/README.md"
      provides: "Styleguide-conforming README with adapter pattern for charting libraries"
      contains: "## Overview"
    - path: "libraries/relay-chronicle/README.md"
      provides: "Styleguide-conforming README for relay event history"
      contains: "## Overview"
    - path: "libraries/uptime-kuma-monitor/README.md"
      provides: "Styleguide-conforming README for uptime monitoring CLI and library"
      contains: "## Overview"
  key_links:
    - from: "libraries/relay-charts/README.md"
      to: "libraries/relay-chronicle/README.md"
      via: "Related Packages — relay-charts depends on relay-chronicle for data"
      pattern: "relay-chronicle"
---

<objective>
Write styleguide-conforming README.md files for the application-layer utility libraries: nostrawl (queue-based crawler), relay-charts (metric visualization), relay-chronicle (event history), and uptime-kuma-monitor (uptime monitoring).

Purpose: These libraries sit at the application layer, closest to end-user functionality. relay-charts and relay-chronicle have rich existing READMEs (419 and 792 lines respectively) that need restructuring to the styleguide. relay-charts has its own adapter system (Chart.js/ECharts/Recharts). nostrawl and uptime-kuma-monitor are simpler rewrites.
Output: Four README.md files that pass markdownlint-cli2 and render in VitePress.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@docs/styleguide/README.md
@.markdownlint-cli2.jsonc
@.planning/phases/03-library-package-readmes/03-CONTEXT.md
@.planning/phases/03-library-package-readmes/03-RESEARCH.md

<interfaces>
<!-- nostrawl: existing README 95 lines. Package: nostrawl v0.1.9 (NOT @nostrwatch/ scoped!) -->
<!-- nostrawl: wraps nostr-fetch with PQueue/BullMQ adapters for queue-based crawling -->

<!-- relay-charts: existing README 419 lines (rich content, wrong structure). Package: @nostrwatch/relay-charts v0.1.0 -->
<!-- relay-charts: adapter system for Chart.js/ECharts/Recharts charting libraries -->
<!-- relay-charts: depends on relay-chronicle for data -->

<!-- relay-chronicle: existing README 792 lines (rich content, wrong structure). Package: @nostrwatch/relay-chronicle v1.0.0 -->
<!-- relay-chronicle: historical relay event management -->

<!-- uptime-kuma-monitor: existing README 67 lines. Package: @nostrwatch/kuma v0.1.0 -->
<!-- uptime-kuma-monitor: CLI + library API, uses nocap websocket adapter -->

<!-- IMPORTANT: Existing READMEs for relay-chronicle (792 lines) and relay-charts (419 lines) -->
<!-- have rich content but wrong structure — they lack MD043 sections, use emoji feature lists. -->
<!-- Strategy: extract accurate technical content, rewrite structure to styleguide. -->
<!-- Do NOT copy format — only copy accurate technical content. -->

<!-- Badge template for published libraries: npm badge, license, status, runtime -->
<!-- Code style: no semicolons, single quotes, ESM imports, TypeScript -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write READMEs for libraries/nostrawl and libraries/uptime-kuma-monitor</name>
  <files>libraries/nostrawl/README.md, libraries/uptime-kuma-monitor/README.md</files>
  <action>
**nostrawl:**
Read source:
- `libraries/nostrawl/package.json` for metadata (NOTE: package name is `nostrawl` v0.1.9, NOT @nostrwatch/ scoped)
- `libraries/nostrawl/src/index.ts` or main source for exports
- Existing README (95 lines) for accurate content
- Look for PQueue and BullMQ adapter patterns

Write `libraries/nostrawl/README.md`:
1. **Header**: `# nostrawl` (no @nostrwatch/ scope — check package.json). npm badge (nostrawl), license badge, status badge (alpha), runtime badge (node)
2. **Overview**: Queue-based Nostr relay web crawler. Wraps nostr-fetch with PQueue (in-memory) or BullMQ (Redis-backed) queue adapters for controlled, rate-limited crawling of relay data.
3. **Installation**: `pnpm add nostrawl` + npm alternative
4. **Quick Start**: Show setting up a crawler with PQueue adapter. TypeScript.
5. **API**: Document the crawler class, queue adapter options, crawl configuration.
6. **Known Limitations**: "No known limitations at this time."
7. **Agent Skills**: "No agent skills defined yet for this package."
8. **Related Packages**: Link to libraries/route66 (state management for crawled data), apps/trawler (primary consumer)
9. **License**: `[MIT](../../LICENSE)`

**uptime-kuma-monitor:**
Read source:
- `libraries/uptime-kuma-monitor/package.json` for metadata (@nostrwatch/kuma v0.1.0)
- Source files for CLI and library API
- Existing README (67 lines) for accurate content

Write `libraries/uptime-kuma-monitor/README.md`:
1. **Header**: `# @nostrwatch/kuma` (use actual package name from package.json). npm badge (kuma), license badge, status badge (alpha), runtime badge (cli)
2. **Overview**: Uptime Kuma integration for Nostr relay monitoring. Provides both a CLI tool and a library API for pushing relay check results to an Uptime Kuma instance. Uses the nocap websocket adapter for relay checks.
3. **Installation**: `pnpm add @nostrwatch/kuma` + npm alternative. Note CLI usage with npx.
4. **Quick Start**: Show CLI usage (npx) and/or library API usage. TypeScript for library API.
5. **API**: Document both CLI flags and library API.
6. **Known Limitations**: "No known limitations at this time."
7. **Agent Skills**: "No agent skills defined yet for this package."
8. **Related Packages**: Link to libraries/nocap (relay checking), libraries/websocket (WebSocket connectivity)
9. **License**: `[MIT](../../LICENSE)`
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/nostrawl/README.md libraries/uptime-kuma-monitor/README.md</automated>
  </verify>
  <done>libraries/nostrawl/README.md and libraries/uptime-kuma-monitor/README.md exist, pass markdownlint-cli2, use correct package names from package.json</done>
</task>

<task type="auto">
  <name>Task 2: Write README for libraries/relay-charts</name>
  <files>libraries/relay-charts/README.md</files>
  <action>
Read relay-charts source:
- `libraries/relay-charts/package.json` for metadata (@nostrwatch/relay-charts v0.1.0)
- `libraries/relay-charts/src/index.ts` for public exports
- Existing README at `libraries/relay-charts/README.md` (419 lines) — extract accurate API content but rewrite structure entirely
- Look for adapter pattern: Chart.js, ECharts, Recharts adapters
- Identify relationship with relay-chronicle

Write `libraries/relay-charts/README.md` following the styleguide:

1. **Header**: `# @nostrwatch/relay-charts` with npm badge, license badge, status badge (alpha), runtime badge (browser)
2. **Overview**: Relay metric visualization with pluggable charting library adapters. Generates charts from relay monitoring data (uptime, latency, NIP support) using Chart.js, ECharts, or Recharts as the rendering backend. Define terms inline. Note dependency on relay-chronicle for data sourcing.
3. **Installation**: `pnpm add @nostrwatch/relay-charts` + npm alternative. Note peer dependency on chosen charting library.
4. **Quick Start**: Show creating a chart from relay data using one adapter (e.g., Chart.js). TypeScript, no semicolons.
5. **API**: Document:
   - Main chart creation API
   - Adapter registration pattern (Chart.js/ECharts/Recharts)
   - Chart types available (uptime, latency, NIP support, etc.)
   - Data format expected from relay-chronicle
   Extract accurate API details from the existing 419-line README.
6. **Known Limitations**: "No known limitations at this time." (no CONCERNS.md entries)
7. **Agent Skills**: "No agent skills defined yet for this package."
8. **Related Packages**: Link to libraries/relay-chronicle (data source), libraries/route66 (state management), apps/gui (primary consumer)
9. **License**: `[MIT](../../LICENSE)`

IMPORTANT: Do NOT copy the existing README's emoji-heavy formatting or feature-list structure. Extract the accurate technical content and restructure to styleguide format. Remove emoji bullet points.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/relay-charts/README.md</automated>
  </verify>
  <done>libraries/relay-charts/README.md exists, passes markdownlint-cli2, documents adapter system for Chart.js/ECharts/Recharts, no emoji formatting</done>
</task>

<task type="auto">
  <name>Task 3: Write README for libraries/relay-chronicle</name>
  <files>libraries/relay-chronicle/README.md</files>
  <action>
Read relay-chronicle source:
- `libraries/relay-chronicle/package.json` for metadata (@nostrwatch/relay-chronicle v1.0.0)
- `libraries/relay-chronicle/src/index.ts` for public exports
- Existing README at `libraries/relay-chronicle/README.md` (792 lines) — extract accurate API content but rewrite structure entirely
- Look for key classes, event storage/query patterns, period management

Write `libraries/relay-chronicle/README.md` following the styleguide:

1. **Header**: `# @nostrwatch/relay-chronicle` with npm badge, license badge, status badge (alpha), runtime badge (node | browser)
2. **Overview**: Historical relay event management. Stores, aggregates, and queries relay monitoring events over time. Manages "chronicle periods" — time windows that capture relay state transitions (online/offline, NIP support changes, latency shifts). Consumes NIP-66 kind 1066 events (relay status deltas). Define terms inline.
3. **Installation**: `pnpm add @nostrwatch/relay-chronicle` + npm alternative
4. **Quick Start**: Show creating a chronicle instance and querying relay history. TypeScript, no semicolons.
5. **API**: Document:
   - Main chronicle class — constructor, configuration
   - Period management — creating, querying, aggregating time periods
   - Event ingestion — adding events, batch operations
   - Query API — filtering by relay, time range, status
   Extract accurate API details from the existing 792-line README. Prioritize the most important 10-15 exports.
6. **Known Limitations**: "No known limitations at this time." (no CONCERNS.md entries for relay-chronicle directly, though route66's ChronicleService has an RTT extraction issue — that's in route66's README, not here)
7. **Agent Skills**: "No agent skills defined yet for this package."
8. **Related Packages**: Link to libraries/relay-charts (visualization of chronicle data), libraries/route66 (ChronicleService wrapper), libraries/nip66 (NIP-66 event kinds consumed), apps/gui (displays chronicle data)
9. **License**: `[MIT](../../LICENSE)`

IMPORTANT: The existing 792-line README has rich content but uses emoji bullets, lacks MD043 sections, and has wrong structure. Extract technical accuracy, discard formatting. This is the most content-heavy rewrite in the phase.
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/relay-chronicle/README.md</automated>
  </verify>
  <done>libraries/relay-chronicle/README.md exists, passes markdownlint-cli2, documents chronicle period management and query API, no emoji formatting, all required sections present</done>
</task>

</tasks>

<verification>
Run markdownlint-cli2 across all four files:

```sh
cd /home/sandwich/Develop/nostr-watch && npx markdownlint-cli2 libraries/nostrawl/README.md libraries/relay-charts/README.md libraries/relay-chronicle/README.md libraries/uptime-kuma-monitor/README.md
```

All four must pass with zero errors.
</verification>

<success_criteria>
- Four README.md files created
- All pass markdownlint-cli2
- relay-charts README documents adapter system without emoji formatting
- relay-chronicle README preserves technical accuracy from 792-line original without emoji formatting
- nostrawl uses correct non-scoped package name
- uptime-kuma-monitor uses correct package name @nostrwatch/kuma
- relay-charts and relay-chronicle cross-link in Related Packages
- Code examples follow styleguide conventions
</success_criteria>

<output>
After completion, create `.planning/phases/03-library-package-readmes/03-07-SUMMARY.md`
</output>
