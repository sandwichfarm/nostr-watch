{
  "name": "@nostrwatch/worker-relay",
  "version": "1.3.1",
  "description": "A nostr relay in a service worker",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "module": "src/index.ts",
  "repository": "https://git.v0l.io/Kieran/snort",
  "author": "Kieran",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf dist",
    "tsc": "tsc",
    "build": "pnpm clean && pnpm tsc && node build.mjs && pnpm run copy:wasm",
    "copy:wasm": "cp src/sqlite/sqlite3.wasm dist/sqlite/sqlite3.wasm"
  },
  "files": [
    "src",
    "dist"
  ],
  "dependencies": {
    "@nostrwatch/utils": "workspace:^",
    "@sqlite.org/sqlite-wasm": "^3.46.1-build3",
    "eventemitter3": "^5.0.1",
    "uuid": "^14.0.0"
  },
  "devDependencies": {
    "@types/copyfiles": "^2",
    "@types/debug": "^4.1.12",
    "@types/sharedworker": "^0.0.112",
    "@types/uuid": "^9.0.7",
    "copyfiles": "^2.4.1",
    "esbuild": "^0.28.1",
    "typescript": "^5.2.2"
  }
}
