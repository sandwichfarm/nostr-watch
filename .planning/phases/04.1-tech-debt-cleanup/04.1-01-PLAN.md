---
phase: 04.1-tech-debt-cleanup
plan: 01
type: gap_closure
wave: 1
depends_on: []
files_modified:
  - internal/seed/README.md
  - .planning/codebase/CONCERNS.md
  - apps/nocapd/package.json
  - internal/nwcache/package.json
autonomous: true
requirements:
  - INT-09
  - LIMIT-01
  - FOUND-08
  - LIMIT-02

must_haves:
  truths:
    - "internal/seed/README.md line 167 links to ../../libraries/db/README.md (not ../../internal/db/README.md) and lychee resolves the link without error"
    - "All 10 CONCERNS.md anchor fragments referenced from route66, auditor, gui, and rstate READMEs resolve to actual ### subheadings in .planning/codebase/CONCERNS.md"
    - "apps/nocapd/package.json contains a nostrwatch.deprecated field set to true and VitePress PackageIndex renders nocapd as deprecated"
    - "internal/nwcache/package.json contains a nostrwatch.deprecated field set to true and VitePress PackageIndex renders nwcache as deprecated"
    - "No existing ## section structure in CONCERNS.md is altered -- only ### subheadings are added beneath existing ## sections"
    - "apps/nocapd/package.json remains valid JSON after the edit"
    - "internal/nwcache/package.json remains valid JSON after the edit"
  artifacts:
    - path: "internal/seed/README.md"
      provides: "Corrected relative link to @nostrwatch/db"
      contains: "../../libraries/db/README.md"
    - path: ".planning/codebase/CONCERNS.md"
      provides: "### subheadings for all 10 referenced anchor fragments"
      contains: "### Hardcoded Filter Limits"
    - path: "apps/nocapd/package.json"
      provides: "nostrwatch.deprecated metadata for PackageIndex rendering"
      contains: "nostrwatch"
    - path: "internal/nwcache/package.json"
      provides: "nostrwatch.deprecated metadata for PackageIndex rendering"
      contains: "nostrwatch"
  key_links:
    - from: "internal/seed/README.md"
      to: "libraries/db/README.md"
      via: "Related Packages link"
      pattern: "../../libraries/db/README.md"
    - from: "libraries/route66/README.md"
      to: ".planning/codebase/CONCERNS.md#hardcoded-filter-limits"
      via: "Known Limitations anchor fragment"
      pattern: "### Hardcoded Filter Limits"
    - from: "libraries/route66/README.md"
      to: ".planning/codebase/CONCERNS.md#default-relay-configuration"
      via: "Known Limitations anchor fragment"
      pattern: "### Default Relay Configuration"
    - from: "libraries/route66/README.md"
      to: ".planning/codebase/CONCERNS.md#incomplete-rtt-extraction"
      via: "Known Limitations anchor fragment"
      pattern: "### Incomplete RTT Extraction"
    - from: "libraries/auditor/README.md"
      to: ".planning/codebase/CONCERNS.md#incomplete-filter-range-testing"
      via: "Known Limitations anchor fragment"
      pattern: "### Incomplete Filter Range Testing"
    - from: "apps/gui/README.md"
      to: ".planning/codebase/CONCERNS.md#gui-table-configuration-separation"
      via: "Known Limitations anchor fragment"
      pattern: "### GUI Table Configuration Separation"
    - from: "apps/gui/README.md"
      to: ".planning/codebase/CONCERNS.md#worker-based-computation-fallback-issues"
      via: "Known Limitations anchor fragment"
      pattern: "### Worker-based Computation Fallback Issues"
    - from: "apps/gui/README.md"
      to: ".planning/codebase/CONCERNS.md#gui-store-initialization-race-conditions"
      via: "Known Limitations anchor fragment"
      pattern: "### GUI Store Initialization Race Conditions"
    - from: "apps/rstate/README.md"
      to: ".planning/codebase/CONCERNS.md#sdk-stub-dependencies"
      via: "Known Limitations anchor fragment"
      pattern: "### SDK Stub Dependencies"
    - from: "apps/rstate/README.md"
      to: ".planning/codebase/CONCERNS.md#outdated-development-utilities"
      via: "Known Limitations anchor fragment"
      pattern: "### Outdated Development Utilities"
    - from: "apps/rstate/README.md"
      to: ".planning/codebase/CONCERNS.md#monitor-coverage-calculation-debug-logging"
      via: "Known Limitations anchor fragment"
      pattern: "### Monitor Coverage Calculation Debug Logging"
---

<objective>
Fix all three integration gaps identified by the v1.0 milestone audit: broken relative link in internal/seed/README.md (CI blocker), missing ### subheadings in CONCERNS.md for 10 anchor fragments referenced by 4 READMEs, and missing nostrwatch.deprecated metadata in nocapd and nwcache package.json files.

Purpose: Close all tech debt from the v1.0 audit so CI passes cleanly, CONCERNS.md anchor navigation works, and the VitePress PackageIndex correctly renders deprecated packages.
Output: Four corrected files -- seed README, CONCERNS.md, nocapd package.json, nwcache package.json.
</objective>

<execution_context>
@/home/sandwich/.claude/get-shit-done/workflows/execute-plan.md
@/home/sandwich/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04.1-tech-debt-cleanup/04.1-RESEARCH.md
@.planning/codebase/CONCERNS.md
@.planning/v1.0-MILESTONE-AUDIT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix broken relative link in internal/seed/README.md</name>
  <files>internal/seed/README.md</files>
  <action>
Fix the broken relative link on line 167 of `internal/seed/README.md`.

**Current (broken):**
```markdown
- [`@nostrwatch/db`](../../internal/db/README.md) — provides the SQLite database that the `db` and `cache` seed sources read from
```

**Fixed:**
```markdown
- [`@nostrwatch/db`](../../libraries/db/README.md) — provides the SQLite database that the `db` and `cache` seed sources read from
```

**Why:** The `db` package lives at `libraries/db/`, not `internal/db/`. There is no `internal/db/` directory. The relative path from `internal/seed/README.md` is `../../` to reach the monorepo root, then `libraries/db/README.md`. This is a CI blocker -- lychee will fail on any PR that touches this file.

**Change:** Replace `../../internal/db/README.md` with `../../libraries/db/README.md` on line 167. This is a single string substitution. Do NOT change any other content in the file.

**Verify the target exists:**
```sh
ls /home/sandwich/Develop/nostr-watch/libraries/db/README.md
```
  </action>
  <verify>
    <automated>ls /home/sandwich/Develop/nostr-watch/libraries/db/README.md && grep "../../libraries/db/README.md" /home/sandwich/Develop/nostr-watch/internal/seed/README.md && echo "PASS: link fixed" || echo "FAIL: link not fixed"</automated>
  </verify>
  <done>internal/seed/README.md line 167 links to ../../libraries/db/README.md; lychee can resolve the target</done>
</task>

<task type="auto">
  <name>Task 2: Add ### subheadings to CONCERNS.md for all referenced anchor fragments</name>
  <files>.planning/codebase/CONCERNS.md</files>
  <action>
Convert 10 bold-text concern entries in `.planning/codebase/CONCERNS.md` into `###` subheadings so that anchor fragment links from 4 READMEs resolve correctly. Only the specific entries referenced by broken anchors need conversion. Do NOT alter the `##` section structure or any body text.

**Conversion pattern:** Replace `**Entry Name:**` with `### Entry Name` followed by a blank line. Keep all body content (bullet points) under the new heading unchanged.

**Before:**
```markdown
**Hardcoded Filter Limits:**
- Issue: MAX_FILTERS hardcoded as 10...
```

**After:**
```markdown
### Hardcoded Filter Limits

- Issue: MAX_FILTERS hardcoded as 10...
```

**Complete list of 10 bold entries to convert to ### headings:**

Under `## Tech Debt`:
1. `**Outdated Development Utilities:**` -> `### Outdated Development Utilities` (line 7, referenced by rstate)
2. `**SDK Stub Dependencies:**` -> `### SDK Stub Dependencies` (line 13, referenced by rstate)
3. `**Hardcoded Filter Limits:**` -> `### Hardcoded Filter Limits` (line 25, referenced by route66)
4. `**Default Relay Configuration:**` -> `### Default Relay Configuration` (line 31, referenced by route66)
5. `**Incomplete Filter Range Testing:**` -> `### Incomplete Filter Range Testing` (line 37, referenced by auditor)
6. `**Incomplete RTT Extraction:**` -> `### Incomplete RTT Extraction` (line 43, referenced by route66)
7. `**GUI Table Configuration Separation:**` -> `### GUI Table Configuration Separation` (line 49, referenced by gui)

Under `## Known Bugs`:
8. `**Monitor Coverage Calculation Debug Logging:**` -> `### Monitor Coverage Calculation Debug Logging` (line 75, referenced by rstate)

Under `## Performance Bottlenecks`:
9. `**Worker-based Computation Fallback Issues:**` -> `### Worker-based Computation Fallback Issues` (line 146, referenced by gui)

Under `## Fragile Areas`:
10. `**GUI Store Initialization Race Conditions:**` -> `### GUI Store Initialization Race Conditions` (line 178, referenced by gui)

**CRITICAL: Do NOT convert bold entries that are NOT referenced by broken anchors.** The following entries should remain as bold text (they are NOT linked from any README):
- `**Unfinished Language Tag Validation:**` (Tech Debt) -- not linked from any README
- `**Relay URL Filtering with Pipe Character:**` (Known Bugs) -- relaymon links to `#known-bugs` section, not this specific entry
- `**Database Inspection Function Incomplete:**` (Known Bugs) -- relaymon links to `#known-bugs` section, not this specific entry
- All entries under Security Considerations, Scaling Limits, Dependencies at Risk, Missing Critical Features, Test Coverage Gaps -- none are linked from READMEs

**Verification of anchor slug computation:** GitHub and VitePress compute anchor slugs by lowercasing heading text and replacing spaces with hyphens.
- `### Hardcoded Filter Limits` -> `#hardcoded-filter-limits` (matches route66 link)
- `### Default Relay Configuration` -> `#default-relay-configuration` (matches route66 link)
- `### Incomplete RTT Extraction` -> `#incomplete-rtt-extraction` (matches route66 link)
- `### Incomplete Filter Range Testing` -> `#incomplete-filter-range-testing` (matches auditor link)
- `### GUI Table Configuration Separation` -> `#gui-table-configuration-separation` (matches gui link)
- `### Worker-based Computation Fallback Issues` -> `#worker-based-computation-fallback-issues` (matches gui link)
- `### GUI Store Initialization Race Conditions` -> `#gui-store-initialization-race-conditions` (matches gui link)
- `### SDK Stub Dependencies` -> `#sdk-stub-dependencies` (matches rstate link)
- `### Outdated Development Utilities` -> `#outdated-development-utilities` (matches rstate link)
- `### Monitor Coverage Calculation Debug Logging` -> `#monitor-coverage-calculation-debug-logging` (matches rstate link)

**Anti-patterns to avoid:**
- Do NOT change `##` headings to `###` -- the `##` section headings (`## Tech Debt`, `## Known Bugs`, etc.) must remain unchanged
- Do NOT add `###` headings for non-referenced entries -- only the 10 listed above
- Do NOT modify body text under any entry
- Do NOT restructure the document beyond these heading promotions
- Ensure a blank line between the new `###` heading and the first bullet point
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && grep -c "^### " .planning/codebase/CONCERNS.md | xargs -I{} sh -c 'if [ {} -ge 10 ]; then echo "PASS: {} ### headings found (expected 10)"; else echo "FAIL: only {} ### headings found (expected 10)"; fi'</automated>
  </verify>
  <done>CONCERNS.md contains exactly 10 new ### subheadings matching the anchor fragments referenced by route66, auditor, gui, and rstate READMEs; all ## section headings are unchanged; body text under each heading is unchanged</done>
</task>

<task type="auto">
  <name>Task 3: Add nostrwatch.deprecated metadata to nocapd and nwcache package.json</name>
  <files>apps/nocapd/package.json, internal/nwcache/package.json</files>
  <action>
Add `"nostrwatch": { "deprecated": true }` to both `apps/nocapd/package.json` and `internal/nwcache/package.json`. This activates the PackageIndex.vue deprecated rendering (opacity 0.6, strikethrough on name, red "deprecated" badge).

**File 1: apps/nocapd/package.json**

Current structure (relevant excerpt):
```json
{
  "name": "@nostrwatch/nocapd",
  "type": "module",
  "version": "1.12.2",
  "main": "src/index.js",
  "license": "MIT",
  "dependencies": { ... },
  "scripts": { ... },
  "devDependencies": { ... }
}
```

Add `"nostrwatch"` key after `"license"`:
```json
{
  "name": "@nostrwatch/nocapd",
  "type": "module",
  "version": "1.12.2",
  "main": "src/index.js",
  "license": "MIT",
  "nostrwatch": {
    "deprecated": true
  },
  "dependencies": { ... },
  ...
}
```

**File 2: internal/nwcache/package.json**

Current structure:
```json
{
  "name": "@nostrwatch/nwcache",
  "version": "0.5.0",
  "type": "module",
  "main": "index.js",
  "license": "MIT",
  "dependencies": { ... }
}
```

Add `"nostrwatch"` key after `"license"`:
```json
{
  "name": "@nostrwatch/nwcache",
  "version": "0.5.0",
  "type": "module",
  "main": "index.js",
  "license": "MIT",
  "nostrwatch": {
    "deprecated": true
  },
  "dependencies": { ... }
}
```

**After editing each file, verify it is valid JSON:**
```sh
node -e "JSON.parse(require('fs').readFileSync('apps/nocapd/package.json', 'utf-8')); console.log('nocapd: valid')"
node -e "JSON.parse(require('fs').readFileSync('internal/nwcache/package.json', 'utf-8')); console.log('nwcache: valid')"
```

**Why both packages:** The v1.0 audit says "Minimum viable fix: Add nostrwatch.deprecated to nocapd and nwcache package.json". nocapd is a deprecated app (replaced by relaymon). nwcache is a deprecated internal package (deprecation stub already exists from Phase 2). Both should render as deprecated in the PackageIndex.

**Anti-patterns to avoid:**
- Do NOT add a trailing comma after the last key-value pair inside a JSON object
- Do NOT modify any other fields in the package.json files
- Do NOT add `nostrwatch.status` -- status defaults to `alpha` which is fine; only `deprecated` is needed
  </action>
  <verify>
    <automated>cd /home/sandwich/Develop/nostr-watch && node -e "const p1 = JSON.parse(require('fs').readFileSync('apps/nocapd/package.json','utf-8')); const p2 = JSON.parse(require('fs').readFileSync('internal/nwcache/package.json','utf-8')); if (p1.nostrwatch?.deprecated === true && p2.nostrwatch?.deprecated === true) { console.log('PASS: both packages have nostrwatch.deprecated = true'); } else { console.log('FAIL:', JSON.stringify({nocapd: p1.nostrwatch, nwcache: p2.nostrwatch})); }"</automated>
  </verify>
  <done>apps/nocapd/package.json and internal/nwcache/package.json both contain "nostrwatch": { "deprecated": true }; both files parse as valid JSON; PackageIndex.vue will render them with deprecated badge and strikethrough</done>
</task>

</tasks>

<verification>
Run all three task verifications in sequence:

1. Verify seed link fix: `grep "../../libraries/db/README.md" internal/seed/README.md`
2. Verify CONCERNS.md headings: `grep -c "^### " .planning/codebase/CONCERNS.md` should return 10
3. Verify package.json metadata: `node -e "const p1=JSON.parse(require('fs').readFileSync('apps/nocapd/package.json','utf-8'));const p2=JSON.parse(require('fs').readFileSync('internal/nwcache/package.json','utf-8'));console.log(p1.nostrwatch?.deprecated===true&&p2.nostrwatch?.deprecated===true?'PASS':'FAIL')"`
4. Run `pnpm lint:docs` to confirm no regressions from CONCERNS.md edits (CONCERNS.md is not in the lint scope, but seed README is)
</verification>

<success_criteria>
- internal/seed/README.md links to ../../libraries/db/README.md (not ../../internal/db/README.md)
- .planning/codebase/CONCERNS.md contains exactly 10 new ### subheadings matching the anchor fragments from route66, auditor, gui, and rstate READMEs
- apps/nocapd/package.json has "nostrwatch": { "deprecated": true } and parses as valid JSON
- internal/nwcache/package.json has "nostrwatch": { "deprecated": true } and parses as valid JSON
- No regressions: pnpm lint:docs passes
</success_criteria>

<output>
After completion, create `.planning/phases/04.1-tech-debt-cleanup/04.1-01-SUMMARY.md`
</output>
